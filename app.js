/* ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   CONSTANTS & STATE
──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────── */
const API = '/api/logs';
const $ = id => document.getElementById(id);
const fmt = v => '₹' + Number(v || 0).toLocaleString('en-IN', { maximumFractionDigits: 0 });
const fmtDate = d => d ? new Date(d + 'T00:00:00').toLocaleDateString('en-IN', { day: '2-digit', month: 'short', year: 'numeric' }) : '';

function _applyBeToggleStyle(btn, active) {
  if (!btn) return;
  if (active) {
    btn.innerHTML = '<span class="be-toggle-icon">🧭</span> Including shelf estimates';
    btn.classList.add('be-toggle-active');
  } else {
    btn.innerHTML = '<span class="be-toggle-icon">📓</span> Tracked sessions only';
    btn.classList.remove('be-toggle-active');
  }
}

function beToggleUntracked(btn) {
  const wasActive = btn.dataset.active === 'true';
  const nowActive = !wasActive;
  btn.dataset.active = nowActive ? 'true' : 'false';
  localStorage.setItem('be_include_untracked', nowActive);
  _applyBeToggleStyle(btn, nowActive);
  renderMoneySaved();
}


const TYPEMETA = {
  'single-estate': { label: 'Single Estate', cls: '' },
  'blend': { label: 'Blend', cls: '' },
  'micro-lot': { label: 'Micro Lot', cls: '' },
  'nano-lot': { label: 'Nano Lot', cls: '' }
};
const BREW_OPTS = ['Espresso Machine', 'V60', 'KaldiPress', 'French Press', 'Cold Brew'];
const STD_DOSE_G = 15; // grams per cup (standard dose assumption)
const SIZE_OPTS = ['100g', '200g', '250g', '500g', '1kg'];

// Process detection — maps keywords in name/notes → canonical process
const PROCESS_MAP = [
  { key: 'natural', label: 'Natural', icon: '🍒', hint: 'sun-dried, dry process' },
  { key: 'washed', label: 'Washed', icon: '💧', hint: 'wet process, fully washed' },
  { key: 'honey', label: 'Honey', icon: '🍯', hint: 'pulped natural, honey process' },
  { key: 'anaerobic', label: 'Anaerobic', icon: '⚗️', hint: 'fermentation, carbonic maceration' },
  { key: 'wet-hulled', label: 'Wet Hulled', icon: '🌾', hint: 'giling basah' },
  { key: 'experimental', label: 'Experimental', icon: '🧪', hint: 'infused, experimental, barrel aged' },
];

let logs = [], orders = [];
let currentFilter = 'all';
let editingId = null;
let itemCount = 0;

// Shelf & journal — all synced to D1, no localStorage
let finishedBags = [];  // derived from bagMeta
let bagMeta = {};       // keyed by log_id (number), loaded from /api/shelf
let journal = [];       // loaded from /api/journal
let jRating = 0;
let jEditId = null;

let pieInst, barInst, lineInst;

function _loadCustomBrewers() {
  try { return JSON.parse(localStorage.getItem('bb_custom_brewers') || '[]'); } catch { return []; }
}
function _saveCustomBrewers(arr) {
  localStorage.setItem('bb_custom_brewers', JSON.stringify(arr));
}
function _loadCustomGrinders() {
  try { return JSON.parse(localStorage.getItem('bb_custom_grinders') || '[]'); } catch { return []; }
}
function _saveCustomGrinders(arr) {
  localStorage.setItem('bb_custom_grinders', JSON.stringify(arr));
}

function toggleCustomBrewerField() {
  const val = $('j-brewer').value;
  const wrap = $('j-brewer-custom-wrap');
  if (wrap) {
    wrap.style.display = val === 'other' ? 'block' : 'none';
    if (val !== 'other') {
      $('j-brewer-custom').value = '';
      $('j-brewer-save').checked = false;
    }
  }
}
function toggleCustomGrinderField() {
  const val = $('j-grinder').value;
  const wrap = $('j-grinder-custom-wrap');
  if (wrap) {
    wrap.style.display = val === 'other' ? 'block' : 'none';
    if (val !== 'other') {
      $('j-grinder-custom').value = '';
      $('j-grinder-save').checked = false;
    }
  }
}
function onGrinderChange() {
  toggleCustomGrinderField();
  const grinderVal = $('j-grinder').value;
  const grindInput = $('j-grind');
  if (grindInput) {
    if (grinderVal === '1Zpresso Q Air') {
      grindInput.placeholder = 'x.y.z (e.g. 3.4.1)';
    } else if (grinderVal === 'Kingrinder K6') {
      grindInput.placeholder = 'e.g. 60 clicks';
    } else {
      grindInput.placeholder = 'e.g. 3.5';
    }
  }
}

function populateBrewerGrinderDropdowns() {
  const defaultBrewers = ['Espresso Machine', 'Americano', 'V60', 'KaldiPress', 'AeroPress', 'French Press', 'Cold Brew', 'Cafflano Kompresso'];
  const customBrewers = _loadCustomBrewers();
  let brewerHTML = '<option value="">Select</option>';
  defaultBrewers.forEach(b => {
    brewerHTML += `<option value="${esc(b)}">${esc(b)}</option>`;
  });
  customBrewers.forEach(b => {
    brewerHTML += `<option value="${esc(b)}">${esc(b)}</option>`;
  });
  brewerHTML += '<option value="other">Other</option>';
  $('j-brewer').innerHTML = brewerHTML;

  const defaultGrinders = ['1Zpresso Q Air', 'Timemore C2/C3', 'Commandante C40', 'Fellow Ode', 'Niche Zero', 'Kingrinder K6', 'Pre-ground'];
  const customGrinders = _loadCustomGrinders();
  let grinderHTML = '<option value="">Select</option>';
  defaultGrinders.forEach(g => {
    grinderHTML += `<option value="${esc(g)}">${esc(g)}</option>`;
  });
  customGrinders.forEach(g => {
    grinderHTML += `<option value="${esc(g)}">${esc(g)}</option>`;
  });
  grinderHTML += '<option value="other">Other</option>';
  $('j-grinder').innerHTML = grinderHTML;
}


/* ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   LOAD DATA FROM API
──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────── */
async function loadData() {
  // Fetch logs (critical) — always renders whatever succeeds
  try {
    const res = await fetch(API);
    const data = await res.json();
    logs = data.logs || [];
    orders = data.orders || [];

  } catch (e) {
    console.error('Logs fetch error:', e);
  }

  // Fetch journal (non-critical)
  try {
    const res = await fetch(API + '?route=journal');
    const data = await res.json();
    journal = (data.journal || []).map(j => ({
      ...j,
      beanId: j.beanId || j.bean_id,
      beanLabel: j.beanLabel || j.bean_label,
      tastes: typeof j.tastes === 'string' ? JSON.parse(j.tastes || '[]') : (j.tastes || []),
    }));
  } catch (e) {
    console.error('Journal fetch error:', e);
  }

  // Fetch shelf meta (non-critical)
  try {
    const res = await fetch(API + '?route=shelf');
    const data = await res.json();
    bagMeta = {};
    (data.shelf || []).forEach(s => {
      const id = s.log_id;
      bagMeta[id] = {
        roastDate: s.roast_date || '',
        deliveredDate: s.delivered_date || '',
        openedDate: s.opened_date || '',
        finishedDate: s.finished_date || '',
        isFinished: !!s.is_finished,
        restDays: s.rest_days != null ? Number(s.rest_days) : null,
        gramEntries: typeof s.gram_entries === 'string'
          ? JSON.parse(s.gram_entries || '[]')
          : (s.gram_entries || []),
      };
    });
    finishedBags = Object.entries(bagMeta)
      .filter(([, m]) => m.isFinished)
      .map(([id]) => Number(id));
  } catch (e) {
    console.error('Shelf fetch error:', e);
  }
  // Data loaded — page-specific render is called by init()
}

// Helper: persist a bag's meta to D1
async function _syncShelf(id) {
  const m = bagMeta[id] || {};
  await fetch(API + '?route=shelf', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      log_id: id,
      roastDate: m.roastDate || '',
      deliveredDate: m.deliveredDate || '',
      openedDate: m.openedDate || '',
      finishedDate: m.finishedDate || '',
      isFinished: !!m.isFinished,
      restDays: m.restDays != null ? m.restDays : null,
      gramEntries: m.gramEntries || [],
    }),
  });
}

/* ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   ITEM ROW BUILDER
──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────── */
function addItemRow(prefill = {}) {
  itemCount++;
  const n = itemCount;
  const isCombo = $('inp-combo').checked;
  let existingBrew = [];
  try { existingBrew = JSON.parse(prefill.brew_equip || '[]'); } catch {
    existingBrew = typeof prefill.brew_equip === 'string' ? prefill.brew_equip.split(',').filter(Boolean) : [];
  }
  const sizeInOpts = SIZE_OPTS.includes(prefill.size);
  const div = document.createElement('div');
  div.className = 'item-row'; div.id = `item-${n}`;
  div.innerHTML = `
    <div class="item-row-head">
      <span class="item-num">Item ${n}</span>
      <button class="btn-rm" onclick="removeItem(${n})">✕</button>
    </div>
    <div class="form-row full"><div class="field">
      <label class="form-label">Product Name</label>
      <input type="text" id="item-name-${n}" value="${esc(prefill.name || '')}" placeholder="e.g. Baarbara Washed AA">
    </div></div>
    <div class="form-row">
      <div class="field">
        <label class="form-label">Category</label>
        <select id="item-cat-${n}" onchange="toggleBeansFields(${n})">
          <option value="beans" ${(prefill.category || 'beans') === 'beans' ? 'selected' : ''}>Beans</option>
          <option value="gears" ${prefill.category === 'gears' ? 'selected' : ''}>Gears</option>
          <option value="accessories" ${prefill.category === 'accessories' ? 'selected' : ''}>Accessories</option>
        </select>
      </div>
      <div class="field" id="item-price-wrap-${n}" style="${isCombo ? 'display:none' : ''}">
        <div style="display:flex;justify-content:space-between;align-items:center">
          <label class="form-label">Price (₹)</label>
          <label class="form-label" style="display:flex;align-items:center;gap:0.25rem;cursor:pointer">
            <input type="checkbox" id="item-gift-${n}" ${prefill.is_gift ? 'checked' : ''} onchange="$('item-price-${n}').disabled = this.checked; if(this.checked) $('item-price-${n}').value = 0;">
            Is Gift 🎁
          </label>
        </div>
        <input type="number" id="item-price-${n}" value="${prefill.price || ''}" placeholder="0" step="0.01" ${prefill.is_gift ? 'disabled' : ''}>
      </div>
    </div>
    <div class="beans-only" id="beans-fields-${n}">
      <div class="form-row">
        <div class="field">
          <label class="form-label">Roaster</label>
          <input type="text" id="item-roaster-${n}" value="${esc(prefill.roaster || '')}" placeholder="e.g. Blue Tokai">
        </div>
        <div class="field">
          <label class="form-label">Origin / Region</label>
          <input type="text" id="item-origin-${n}" value="${esc(prefill.origin || '')}" placeholder="e.g. Sidama, Ethiopia">
        </div>
      </div>
      <div class="form-row">
        <div class="field">
          <label class="form-label">Bag Size</label>
          <select id="item-size-${n}" onchange="toggleCustomSize(${n})">
            <option value="">Select</option>
            ${SIZE_OPTS.map(s => `<option value="${s}" ${prefill.size === s ? 'selected' : ''}>${s}</option>`).join('')}
            <option value="custom" ${prefill.size && !sizeInOpts ? 'selected' : ''}>Custom</option>
          </select>
        </div>
        <div class="field" id="cswrap-${n}" style="display:${prefill.size && !sizeInOpts ? 'block' : 'none'}">
          <label class="form-label">Custom Size</label>
          <input type="text" id="item-size-custom-${n}" value="${prefill.size && !sizeInOpts ? esc(prefill.size) : ''}">
        </div>
      </div>
      <div class="field" style="margin-bottom:.65rem">
        <label class="form-label">Coffee Type</label>
        <div class="pill-group" id="type-pills-${n}">
          ${['single-estate', 'blend', 'micro-lot', 'nano-lot'].map(v => `<span class="pill-opt ${prefill.coffee_type === v ? 'selected' : ''}" data-val="${v}" onclick="selectPill(this,'type-pills-${n}')">${TYPEMETA[v].label}</span>`).join('')}
        </div>
      </div>
      <div class="field" style="margin-bottom:.65rem">
        <label class="form-label">Process</label>
        <div class="pill-group" id="process-pills-${n}">
          ${['Natural', 'Washed', 'Honey', 'Anaerobic', 'Wet Hulled', 'Unknown'].map(v => `<span class="pill-opt ${(prefill.process || '') === (v) ? 'selected' : ''}" data-val="${v}" onclick="selectPill(this,'process-pills-${n}');toggleCustomProcess(${n})">${v}</span>`).join('')}
          <span class="pill-opt ${!['Natural', 'Washed', 'Honey', 'Anaerobic', 'Wet Hulled', 'Unknown', ''].includes(prefill.process || '') ? 'selected' : ''}" data-val="__custom__" onclick="selectPill(this,'process-pills-${n}');toggleCustomProcess(${n})">Custom…</span>
        </div>
        <div id="custom-process-wrap-${n}" style="margin-top:.45rem;display:${!['Natural', 'Washed', 'Honey', 'Anaerobic', 'Wet Hulled', 'Unknown', ''].includes(prefill.process || '') ? 'block' : 'none'}">
          <input type="text" id="custom-process-${n}" placeholder="e.g. Carbonic Maceration, Extended Ferment…" value="${esc(!['Natural', 'Washed', 'Honey', 'Anaerobic', 'Wet Hulled', 'Unknown', ''].includes(prefill.process || '') ? prefill.process || '' : '')}">
        </div>
      </div>

    </div>
    <div class="field">
      <label class="form-label">Notes</label>
      <textarea id="item-notes-${n}" placeholder="Origin, tasting notes, process details…">${esc(prefill.notes || '')}</textarea>
    </div>`;
  $('itemsList').appendChild(div);
  toggleBeansFields(n);
  return n;
}

function removeItem(n) {
  const el = $(`item-${n}`);
  if (el) el.remove();
  if ($('itemsList').children.length === 0) addItemRow();
}
function toggleBeansFields(n) {
  const cat = $(`item-cat-${n}`)?.value;
  $(`beans-fields-${n}`)?.classList.toggle('hidden', cat !== 'beans');
}
function toggleCustomSize(n) {
  if ($(`cswrap-${n}`)) $(`cswrap-${n}`).style.display = $(`item-size-${n}`)?.value === 'custom' ? 'block' : 'none';
}
function toggleCombo() {
  const isCombo = $('inp-combo').checked;
  $('comboPriceWrap').classList.toggle('active', isCombo);
  document.querySelectorAll('[id^="item-price-wrap-"]').forEach(el => el.style.display = isCombo ? 'none' : '');
}
function selectPill(el, gid) {
  const already = el.classList.contains('selected');
  document.querySelectorAll(`#${gid} .pill-opt:not(.multi)`).forEach(p => p.classList.remove('selected'));
  if (!already) el.classList.add('selected');
}
function getPill(gid) { return document.querySelector(`#${gid} .pill-opt.selected`)?.dataset.val || ''; }
function getMulti(gid) { return [...document.querySelectorAll(`#${gid} .multi.selected`)].map(p => p.dataset.val); }
function toggleCustomProcess(n) {
  const sel = getPill(`process-pills-${n}`);
  const wrap = $(`custom-process-wrap-${n}`);
  if (wrap) wrap.style.display = sel === '__custom__' ? 'block' : 'none';
}
function getItemData(n) {
  const sizeRaw = $(`item-size-${n}`)?.value || '';
  return {
    category: $(`item-cat-${n}`)?.value || 'beans',
    name: ($(`item-name-${n}`)?.value || '').trim(),
    price: parseFloat($(`item-price-${n}`)?.value) || 0,
    is_gift: $(`item-gift-${n}`)?.checked || false,
    notes: ($(`item-notes-${n}`)?.value || '').trim(),
    roaster: ($(`item-roaster-${n}`)?.value || '').trim(),
    origin: ($(`item-origin-${n}`)?.value || '').trim(),
    size: sizeRaw === 'custom' ? ($(`item-size-custom-${n}`)?.value || '').trim() : sizeRaw,
    coffee_type: getPill(`type-pills-${n}`),
    process: (() => {
      const v = getPill(`process-pills-${n}`);
      if (v === '__custom__') return ($(`custom-process-${n}`)?.value || '').trim() || '';
      return v;
    })(),
    brew_equip: [],  // captured via journal entries instead
    qty: 1
  };
}
function esc(s) { return String(s).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;'); }

/* ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   SAVE / EDIT / DELETE
──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────── */
async function saveOrder() {
  const date = $('inp-date').value;
  const vendor = $('inp-vendor').value.trim();
  const orderId = $('inp-order').value.trim();
  const isCombo = $('inp-combo').checked;
  const comboPrice = parseFloat($('inp-combo-price').value) || 0;
  if (!date || !vendor) { alert('Please fill in date and vendor.'); return; }
  const itemEls = [...$('itemsList').children];
  const items = [];
  for (const el of itemEls) {
    const n = el.id.replace('item-', '');
    const d = getItemData(n);
    if (!d.name) { alert('Every item needs a name.'); return; }
    items.push({ ...d, brew_equip: JSON.stringify(d.brew_equip) });
  }
  if (!items.length) { alert('Add at least one item.'); return; }

  if (editingId) {
    const item = items[0];
    try {
      const res = await fetch(API, {
        method: 'PUT', headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          id: editingId, order_id: orderId, date, vendor,
          category: item.category, name: item.name, price: isCombo ? 0 : item.price,
          is_gift: item.is_gift,
          notes: item.notes, roaster: item.roaster, size: item.size,
          coffee_type: item.coffee_type, brew_equip: item.brew_equip, qty: 1,
          process: item.process, origin: item.origin,
          is_combo: isCombo, combo_price: comboPrice
        })
      });
      if (res.ok) { await loadData(); window.location.href = '/log'; }
      else alert('Save failed: ' + await res.text());
    } catch (e) { alert('Error: ' + e.message); }
  } else {
    try {
      const res = await fetch(API, {
        method: 'POST', headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ order: { order_id: orderId, date, vendor, is_combo: isCombo, combo_price: comboPrice, notes: '' }, items })
      });
      if (res.ok) { await loadData(); window.location.href = '/log'; }
      else alert('Save failed: ' + await res.text());
    } catch (e) { alert('Error: ' + e.message); }
  }
}

function resetForm() {
  if (!$('addPageTitle')) return;
  editingId = null; itemCount = 0;
  $('addPageTitle').innerHTML = 'New <em>Order</em>';
  $('editBanner').classList.remove('active');
  $('inp-date').valueAsDate = new Date();
  $('inp-order').value = ''; $('inp-vendor').value = '';
  $('inp-combo').checked = false; $('inp-combo-price').value = '';
  $('comboPriceWrap').classList.remove('active');
  $('itemsList').innerHTML = ''; addItemRow();
}

async function editEntry(id) {
  if (document.body.dataset.page !== 'add') { window.location.href = '/add?edit=' + id; return; }
  const entry = logs.find(l => l.id === id);
  if (!entry) return;
  editingId = id;
  $('addPageTitle').innerHTML = 'Edit <em>Entry</em>';
  $('editBanner').textContent = `Editing "${entry.name}". Update and save.`;
  $('editBanner').classList.add('active');
  $('inp-date').value = entry.date; $('inp-order').value = entry.order_id || '';
  $('inp-vendor').value = entry.vendor;
  const isCombo = entry.is_combo === 1;
  $('inp-combo').checked = isCombo; $('inp-combo-price').value = entry.combo_price || '';
  $('comboPriceWrap').classList.toggle('active', isCombo);
  $('itemsList').innerHTML = ''; itemCount = 0;
  addItemRow(entry);
}

async function deleteEntry(id) {
  if (!confirm('Remove this entry?')) return;
  try {
    const res = await fetch(API, { method: 'DELETE', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ id }) });
    if (res.ok) { await loadData(); renderEntries(); }
  } catch (e) { alert('Delete failed: ' + e.message); }
}

/* ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── 
   RENDER: LOG
──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ────────────────────────────────────────────────────────────────────────────────  */
function renderStats() {
  const beans = logs.filter(l => l.category === 'beans').reduce((s, l) => s + Number(l.price || 0), 0);
  const gears = logs.filter(l => l.category === 'gears').reduce((s, l) => s + Number(l.price || 0), 0);
  const acc = logs.filter(l => l.category === 'accessories').reduce((s, l) => s + Number(l.price || 0), 0);
  const total = beans + gears + acc;
  if ($('grandTotal')) $('grandTotal').textContent = fmt(total);
  $('statBeans').textContent = fmt(beans);
  $('statGears').textContent = fmt(gears);
  $('statAcc').textContent = fmt(acc);
  $('statCount').textContent = logs.length;
}

function renderEntries() {
  let list = currentFilter === 'all' ? logs : logs.filter(l => l.category === currentFilter);
  list = [...list].sort((a, b) => new Date(b.date) - new Date(a.date));
  if (!list.length) { $('entriesList').innerHTML = '<div class="empty-state"><div class="icon">☕</div><p>No entries yet.</p></div>'; return; }

  const groups = {};
  list.forEach(l => {
    const k = (l.order_id || '').trim() || `solo-${l.id}`;
    (groups[k] = groups[k] || []).push(l);
  });
  const keys = Object.keys(groups).sort((a, b) => new Date(groups[b][0]?.date) - new Date(groups[a][0]?.date));

  let html = '';
  keys.forEach(k => {
    const g = groups[k];
    const isSolo = k.startsWith('solo-');
    const orderObj = orders.find(o => o.order_id === k);
    const isCombo = orderObj?.is_combo === 1;
    const comboPrice = orderObj?.combo_price || 0;
    const groupTotal = isCombo ? comboPrice : g.reduce((s, l) => s + Number(l.price || 0), 0);

    if (isSolo) {
      const l = g[0];
      html += renderEntryCard(l, false, 'solo-card type-' + l.category);
    } else {
      html += `<div class="order-group">
        <div class="order-group-header">
          <div class="og-left">
            <span class="og-id">#${k}</span>
            ${isCombo ? '<span class="combo-badge">Combo</span>' : ''}
            <span class="og-meta">${g[0]?.vendor || ''} · ${fmtDate(g[0]?.date)}</span>
          </div>
          <div class="og-actions">
            <span class="og-total">${fmt(groupTotal)}</span>
          </div>
        </div>`;
      g.forEach(l => { html += renderEntryCard(l, isCombo, 'entry-card type-' + l.category); });
      html += '</div>';
    }
  });
  $('entriesList').innerHTML = html;
}

function renderEntryCard(l, isCombo, cls) {
  const tag = l.category === 'beans' ? 'tag-beans' : l.category === 'gears' ? 'tag-gears' : 'tag-accessories';
  const lbl = l.category === 'beans' ? 'Beans' : l.category === 'gears' ? 'Gears' : 'Accessories';
  const pills = [];
  if (l.origin && l.category === 'beans') pills.push(`<span class="epill origin">📍 ${esc(l.origin)}</span>`);
  if (l.roaster && l.category === 'beans') pills.push(`<span class="epill">${esc(l.roaster)}</span>`);
  if (l.size) pills.push(`<span class="epill">${esc(l.size)}</span>`);
  if (l.coffee_type && TYPEMETA[l.coffee_type]) pills.push(`<span class="epill">${TYPEMETA[l.coffee_type].label}</span>`);
  const proc = detectProcess(l);
  if (proc) pills.push(`<span class="epill process">${proc.icon} ${proc.label}</span>`);
  getBrew(l).forEach(b => pills.push(`<span class="epill brew">☕ ${esc(b)}</span>`));
  const priceHtml = l.is_gift ? `<span class="entry-price" style="color:var(--green)">🎁 Gift</span>` : isCombo ? `<span class="entry-price combo-part">combo</span>` : `<span class="entry-price">${fmt(l.price)}</span>`;
  return `<div class="${cls}">
    <div class="entry-left">
      <div class="entry-name">${esc(l.name)}</div>
      <div class="entry-meta">
        <span class="entry-vendor">${esc(l.vendor)}</span>
        <span class="entry-date">${fmtDate(l.date)}</span>
        <span class="entry-tag ${tag}">${lbl}</span>
      </div>
      ${pills.length ? `<div class="entry-pills">${pills.join('')}</div>` : ''}
      ${l.notes ? `<div class="entry-notes">${esc(l.notes)}</div>` : ''}
    </div>
    <div class="entry-right">
      ${priceHtml}
      <div style="display:flex;gap:.3rem">
        <button class="action-btn" onclick="editEntry(${l.id})">Edit</button>
        <button class="action-btn del" onclick="deleteEntry(${l.id})">✕</button>
      </div>
    </div>
  </div>`;
}

function setFilter(f, btn) {
  currentFilter = f;
  document.querySelectorAll('.filter-btn').forEach(b => b.classList.remove('active'));
  btn.classList.add('active');
  renderEntries();
}

let currentShelfFilter = 'available';
function setShelfFilter(f, btn) {
  currentShelfFilter = f;
  document.querySelectorAll('#page-shelf .filter-btn').forEach(b => b.classList.remove('active'));
  btn.classList.add('active');
  renderShelf();
}

/* ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── 
   RENDER: SHELF
──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ──────────────────────────────────────────────────────────────────────────────── ────────────────────────────────────────────────────────────────────────────────  */
function renderShelf() {
  const beans = logs.filter(l => l.category === 'beans').sort((a, b) => new Date(b.date) - new Date(a.date));
  const finished = beans.filter(b => finishedBags.includes(b.id));
  const unfinished = beans.filter(b => !finishedBags.includes(b.id));

  const opened = unfinished.filter(b => bagMeta[b.id] && bagMeta[b.id].openedDate);
  const sealed = unfinished.filter(b => !bagMeta[b.id] || !bagMeta[b.id].openedDate);

  // Header = total bags ever purchased
  $('shelfCount').textContent = beans.length;
  $('shelfActive').textContent = opened.length;
  $('shelfSealedCard').textContent = sealed.length;

  // ── Low-bean alert + cups predictor ─────────────────────────────
  const LOW_BEAN_THRESHOLD = 300; // grams

  // Calculate available grams for each unfinished bag
  let totalAvailableG = 0;
  let cupsBreakdown = [];
  let totalCupsLeft = 0;
  unfinished.forEach(l => {
    const meta = bagMeta[l.id] || {};
    const bagSizeG = parseBagSize(l.size);
    const entries = meta.gramEntries || [];
    const usedG = entries.reduce((s, e) => s + Number(e.grams || 0), 0);
    const isOpened = !!meta.openedDate;
    let availG = 0;
    if (bagSizeG) {
      availG = Math.max(0, bagSizeG - usedG);
    } else if (!isOpened) {
      // Unknown size sealed bag — skip from total but flag
      availG = 0;
    }
    if (availG > 0) {
      totalAvailableG += availG;
      
      let avgDose = STD_DOSE_G;
      const beanJournals = journal.filter(j => parseInt(j.beanId) === l.id && Number(j.dose) > 0);
      if (beanJournals.length > 0) {
        const sumDose = beanJournals.reduce((s, j) => s + Number(j.dose), 0);
        avgDose = sumDose / beanJournals.length;
      } else {
        const doseEntries = entries.filter(e => Number(e.grams) > 0 && Number(e.grams) < 40);
        if (doseEntries.length > 0) {
          const sumDose = doseEntries.reduce((s, e) => s + Number(e.grams), 0);
          avgDose = sumDose / doseEntries.length;
        }
      }
      
      const cups = Math.floor(availG / avgDose);
      totalCupsLeft += cups;
      cupsBreakdown.push({ name: l.name, availG, cups, isOpened, avgDose });
    }
  });

  const alertEl = $('shelf-bean-alert');
  if (alertEl) {
    if (totalAvailableG > 0 && totalAvailableG < LOW_BEAN_THRESHOLD) {
      const urgency = totalAvailableG < 100 ? 'critical' : 'warning';
      alertEl.className = `low-bean-alert ${urgency}`;
      alertEl.innerHTML = `
        <div class="lba-icon">${totalAvailableG < 100 ? '🚨' : '⚠️'}</div>
        <div class="lba-body">
          <div class="lba-title">${totalAvailableG < 100 ? 'Critically Low on Beans!' : 'Running Low on Beans'}</div>
          <div class="lba-sub">${totalAvailableG.toFixed(0)}g available across all bags · <strong>~${totalCupsLeft} cups left</strong></div>
          ${cupsBreakdown.length > 1 ? '<div class="lba-breakdown">' + cupsBreakdown.map(b => `<span>${b.isOpened ? '🔓' : '📦'} ${esc(b.name.split(' ').slice(0, 2).join(' '))}: ~${b.cups}c <span style="opacity:0.6;font-size:0.85em">(@${b.avgDose.toFixed(1)}g)</span></span>`).join('') + '</div>' : ''}
        </div>
        <a href="/add" class="lba-cta">+ Order Beans</a>`;
      alertEl.style.display = 'flex';
    } else if (totalAvailableG === 0 && unfinished.length > 0) {
      alertEl.className = 'low-bean-alert critical';
      alertEl.innerHTML = `<div class="lba-icon">☕</div><div class="lba-body"><div class="lba-title">No beans tracked!</div><div class="lba-sub">Track brew usage on bags to see your bean levels.</div></div>`;
      alertEl.style.display = 'flex';
    } else if (totalAvailableG >= LOW_BEAN_THRESHOLD) {
      // Show cups predictor summary even when not low
      alertEl.className = 'low-bean-alert ok';
      alertEl.innerHTML = `
        <div class="lba-icon">✅</div>
        <div class="lba-body">
          <div class="lba-title">Bean Supply OK</div>
          <div class="lba-sub">${totalAvailableG.toFixed(0)}g available · <strong>~${totalCupsLeft} cups ahead</strong></div>
          ${cupsBreakdown.length ? '<div class="lba-breakdown">' + cupsBreakdown.map(b => `<span>${b.isOpened ? '🔓' : '📦'} ${esc(b.name.split(' ').slice(0, 2).join(' '))}: ~${b.cups}c <span style="opacity:0.6;font-size:0.85em">(@${b.avgDose.toFixed(1)}g)</span></span>`).join('') + '</div>' : ''}
        </div>`;
      alertEl.style.display = 'flex';
    } else {
      alertEl.style.display = 'none';
    }
  }

  const renderBag = (l, isDone) => {
    const meta = bagMeta[l.id] || {};
    const proc = detectProcess(l);
    const brew = getBrew(l);
    const pills = [];
    if (l.origin) pills.push(`<span class="epill origin">📍 ${esc(l.origin)}</span>`);
    if (l.roaster) pills.push(`<span class="epill">${esc(l.roaster)}</span>`);
    if (l.size) pills.push(`<span class="epill">${esc(l.size)}</span>`);
    if (l.coffee_type && TYPEMETA[l.coffee_type]) pills.push(`<span class="epill">${TYPEMETA[l.coffee_type].label}</span>`);
    if (proc) pills.push(`<span class="epill process">${proc.icon} ${proc.label}</span>`);
    brew.forEach(b => pills.push(`<span class="epill brew">☕ ${esc(b)}</span>`));

    // Lifecycle date pills
    const lifePills = [];
    if (meta.roastDate) lifePills.push(`<span class="epill" style="background:var(--green-bg);color:var(--green)">🌿 Roasted ${fmtDate(meta.roastDate)}</span>`);
    if (meta.deliveredDate) lifePills.push(`<span class="epill">📦 ${fmtDate(meta.deliveredDate)}</span>`);
    if (meta.openedDate) lifePills.push(`<span class="epill">🔓 ${fmtDate(meta.openedDate)}</span>`);
    if (isDone && meta.finishedDate) lifePills.push(`<span class="epill" style="background:var(--green-bg);color:var(--green)">✅ ${fmtDate(meta.finishedDate)}</span>`);

    // Gram usage bar
    const entries = meta.gramEntries || [];
    const totalUsed = entries.reduce((s, e) => s + Number(e.grams || 0), 0);
    const bagSizeG = parseBagSize(l.size);
    let gramBar = '';
    if (!isDone && totalUsed > 0) {
      if (bagSizeG) {
        const pct = Math.min(100, Math.round(totalUsed / bagSizeG * 100));
        gramBar = `<div class="freshness-bar-wrap">
          <div class="freshness-label">${totalUsed}g brewed · ${bagSizeG - totalUsed > 0 ? (bagSizeG - totalUsed) + 'g left' : 'bag empty'}</div>
          <div class="freshness-track"><div class="freshness-fill ${pct >= 100 ? 'fresh-red' : 'fresh-amber'}" style="width:${pct}%"></div></div>
        </div>`;
      } else {
        gramBar = `<div class="freshness-bar-wrap"><div class="freshness-label">${totalUsed}g brewed total</div></div>`;
      }
    }

    // Freshness bar — two-phase: resting (from roast) + freshness (from bag opened)
    let freshnessBar = '';
    if (!isDone) {
      // Per-bean rest days (user-set or smart default based on process)
      const RESTING_DAYS = meta.restDays != null ? meta.restDays : _defaultRestDays(l);
      const FRESH_AFTER_OPEN = 30; // days of peak freshness after opening
      const SEALED_SHELF_LIFE = 60; // days sealed bags stay fresh from roast
      const now = Date.now();

      const roastDate = meta.roastDate ? new Date(meta.roastDate + 'T00:00:00').getTime() : null;
      const openedDate = meta.openedDate ? new Date(meta.openedDate + 'T00:00:00').getTime() : null;
      const purchaseDate = new Date((l.date || '') + 'T00:00:00').getTime();
      const daysSinceRoast = roastDate ? Math.floor((now - roastDate) / 86400000) : null;
      const daysSinceOpened = openedDate ? Math.floor((now - openedDate) / 86400000) : null;

      if (roastDate && daysSinceRoast < RESTING_DAYS && !openedDate) {
        // ── Phase 1: Still resting / degassing (only if bag not yet opened)
        const restPct = Math.min(100, Math.round(daysSinceRoast / RESTING_DAYS * 100));
        const daysLeft = RESTING_DAYS - daysSinceRoast;
        freshnessBar = `<div class="freshness-bar-wrap">
          <div class="freshness-label">⏳ Resting · ${daysSinceRoast}d of ${RESTING_DAYS}d degassing · ${daysLeft}d left</div>
          <div class="freshness-track"><div class="freshness-fill fresh-blue" style="width:${restPct}%"></div></div>
        </div>`;
      } else if (roastDate && daysSinceRoast >= RESTING_DAYS && !openedDate) {
        // ── Resting complete but bag not opened yet — sealed shelf life
        const sealedDays = daysSinceRoast;
        const sealedPct = Math.max(0, Math.min(100, 100 - Math.round(sealedDays / SEALED_SHELF_LIFE * 100)));
        const sealedCls = sealedPct > 60 ? 'fresh-green' : sealedPct > 30 ? 'fresh-amber' : 'fresh-red';
        const sealedLabel = sealedPct > 60 ? '✅ Ready to open' : sealedPct > 30 ? '⏳ Open soon' : '⚠️ Open now';
        freshnessBar = `<div class="freshness-bar-wrap">
          <div class="freshness-label">${sealedLabel} · Rested ${daysSinceRoast}d since roast · sealed</div>
          <div class="freshness-track"><div class="freshness-fill ${sealedCls}" style="width:${sealedPct}%"></div></div>
        </div>`;
      } else if (openedDate) {
        // ── Phase 2: Bag is opened — freshness countdown from opened date
        // Also factor in roast age at open if available
        let maxFresh = FRESH_AFTER_OPEN;
        let extraInfo = '';
        if (roastDate) {
          const roastAgeAtOpen = Math.floor((openedDate - roastDate) / 86400000);
          const wasRested = roastAgeAtOpen >= RESTING_DAYS;
          extraInfo = wasRested
            ? ` · rested ${roastAgeAtOpen}d`
            : ` · opened early (${roastAgeAtOpen}d rest)`;
          // If opened much later after roast, shorten the fresh window
          if (roastAgeAtOpen > 30) maxFresh = Math.max(14, FRESH_AFTER_OPEN - Math.floor((roastAgeAtOpen - 30) / 3));
        }
        const freshPct = Math.max(0, Math.min(100, 100 - Math.round(daysSinceOpened / maxFresh * 100)));
        const freshCls = freshPct > 60 ? 'fresh-green' : freshPct > 30 ? 'fresh-amber' : 'fresh-red';
        const freshLabel = freshPct > 60 ? 'Peak Fresh' : freshPct > 30 ? 'Use Soon' : 'Getting Stale';
        freshnessBar = `<div class="freshness-bar-wrap">
          <div class="freshness-label">${freshLabel} · ${daysSinceOpened}d since opened${extraInfo}</div>
          <div class="freshness-track"><div class="freshness-fill ${freshCls}" style="width:${freshPct}%"></div></div>
        </div>`;
      } else {
        // ── Fallback: no roast date or opened date — use purchase date
        const daysSincePurchase = Math.floor((now - purchaseDate) / 86400000);
        const fallbackMax = 45;
        const freshPct = Math.max(0, Math.min(100, 100 - Math.round(daysSincePurchase / fallbackMax * 100)));
        const freshCls = freshPct > 60 ? 'fresh-green' : freshPct > 30 ? 'fresh-amber' : 'fresh-red';
        const freshLabel = freshPct > 60 ? 'Likely Fresh' : freshPct > 30 ? 'Check Freshness' : 'Possibly Stale';
        freshnessBar = `<div class="freshness-bar-wrap">
          <div class="freshness-label">${freshLabel} · ${daysSincePurchase}d since purchase</div>
          <div class="freshness-track"><div class="freshness-fill ${freshCls}" style="width:${freshPct}%"></div></div>
        </div>`;
      }
    }

    return `<div class="shelf-card ${isDone ? 'finished' : ''}">
      <div class="shelf-left">
        <div class="shelf-name">${esc(l.name)}</div>
        <div class="shelf-roaster">${esc(l.vendor)}${l.roaster && l.roaster !== l.vendor ? ' · ' + esc(l.roaster) : ''}</div>
        ${pills.length ? `<div class="shelf-pills">${pills.join('')}</div>` : ''}
        ${lifePills.length ? `<div class="shelf-pills" style="margin-top:.32rem">${lifePills.join('')}</div>` : ''}
        ${freshnessBar}
        ${gramBar}
      </div>
      <div class="shelf-right">

        <span class="shelf-date">${fmtDate(l.date)}</span>
        <button class="action-btn" onclick="openBagModal(${l.id})" style="font-size:.68rem;white-space:nowrap">📋 Track</button>
        <button class="btn-finish ${isDone ? 'done' : ''}" onclick="toggleFinished(${l.id})">
          ${isDone ? '↩ Reopen' : '✓ Finished'}
        </button>
      </div>
    </div>`;
  };

  let listToShow = [];
  if (currentShelfFilter === 'available') listToShow = unfinished;
  else listToShow = finished;

  $('shelf-list').innerHTML = listToShow.length
    ? listToShow.map(b => renderBag(b, currentShelfFilter === 'finished')).join('')
    : `<div class="empty-state"><div class="icon">🫙</div><p>No ${currentShelfFilter} beans found.</p></div>`;
}

async function toggleFinished(id) {
  if (!bagMeta[id]) bagMeta[id] = {};
  if (finishedBags.includes(id)) {
    finishedBags = finishedBags.filter(x => x !== id);
    bagMeta[id].finishedDate = '';
    bagMeta[id].isFinished = false;
  } else {
    finishedBags.push(id);
    if (!bagMeta[id].finishedDate) {
      bagMeta[id].finishedDate = new Date().toISOString().slice(0, 10);
    }
    bagMeta[id].isFinished = true;
  }
  await _syncShelf(id);
  renderShelf();
}

/* ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   BAG LIFECYCLE MODAL
──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────── */
function parseBagSize(size) {
  if (!size) return null;
  const m = String(size).match(/(\d+(?:\.\d+)?)\s*g/i);
  if (m) return Number(m[1]);
  const km = String(size).match(/(\d+(?:\.\d+)?)\s*kg/i);
  if (km) return Number(km[1]) * 1000;
  return null;
}

// Smart default rest days based on process
function _defaultRestDays(log) {
  const proc = (log?.process || '').toLowerCase();
  // Espresso-style darker roasts degas faster; lighter & anaerobic/natural need longer
  if (proc.includes('anaerobic') || proc.includes('carbonic')) return 14;
  if (proc.includes('natural')) return 10;
  if (proc.includes('honey')) return 9;
  if (proc.includes('washed')) return 7;
  return 7; // sensible default
}

function openBagModal(id) {
  const meta = bagMeta[id] || {};
  const log = logs.find(l => l.id === id);
  $('bagModalId').value = id;
  $('bagModalTitle').innerHTML = log
    ? `Track <em>${esc(log.name)}</em>`
    : 'Track <em>Bag</em>';
  $('bm-roastDate').value = meta.roastDate || '';
  $('bm-deliveredDate').value = meta.deliveredDate || '';
  $('bm-openedDate').value = meta.openedDate || '';
  $('bm-finishedDate').value = meta.finishedDate || '';
  // Rest days: show saved value or smart default from process
  const restDefault = _defaultRestDays(log);
  $('bm-restDays').value = meta.restDays != null ? meta.restDays : restDefault;
  $('bm-restDays').placeholder = restDefault;
  if (!bagMeta[id]) bagMeta[id] = {};
  if (!bagMeta[id].gramEntries) bagMeta[id].gramEntries = [];
  renderGramEntries(id);
  $('bagModalOverlay').classList.remove('hidden');
  document.body.style.overflow = 'hidden';
}

function closeBagModal(e) {
  if (e && e.target !== $('bagModalOverlay')) return;
  $('bagModalOverlay').classList.add('hidden');
  document.body.style.overflow = '';
}

function _gramTotal(id) {
  return (bagMeta[id]?.gramEntries || []).reduce((s, e) => s + Number(e.grams || 0), 0);
}

function _updateGramUI(id) {
  const log = logs.find(l => l.id === id);
  const bagSizeG = parseBagSize(log?.size);
  const totalUsed = _gramTotal(id);
  if (bagSizeG) {
    const pct = Math.min(100, Math.round(totalUsed / bagSizeG * 100));
    $('bm-gramFill').style.width = pct + '%';
    $('bm-gramFill').className = 'gram-fill' + (totalUsed > bagSizeG ? ' over' : '');
    $('bm-gramTrack').style.display = '';
    const rem = bagSizeG - totalUsed;
    $('bm-gramSummary').textContent = `${totalUsed}g used of ${bagSizeG}g · ${rem > 0 ? rem + 'g remaining' : 'bag empty'}`;
  } else {
    $('bm-gramTrack').style.display = 'none';
    $('bm-gramSummary').textContent = totalUsed > 0 ? `${totalUsed}g used total` : '';
  }
}

function renderGramEntries(id) {
  _updateGramUI(id);
  const entries = bagMeta[id]?.gramEntries || [];
  $('bm-gramHeaders').style.display = entries.length ? 'grid' : 'none';
  const USE_FOR_OPTS = ['Espresso', 'Americano', 'Pour Over', 'French Press', 'Cold Brew', 'AeroPress', 'Gifted', 'Other'];
  $('bm-gramList').innerHTML = entries.map((e, i) => `
    <div class="gram-entry-row" style="grid-template-columns:1fr 80px 120px 34px">
      <input type="date" value="${e.date || ''}"
        onchange="updateGramEntry(${i},'date',this.value)">
      <input type="number" value="${e.grams || ''}" placeholder="g" min="0" step="0.1"
        oninput="updateGramEntry(${i},'grams',this.value)">
      <select onchange="updateGramEntry(${i},'usedFor',this.value)" style="font-size:.75rem;padding:.45rem .5rem">
        <option value="">— for —</option>
        ${USE_FOR_OPTS.map(o => `<option value="${o}" ${e.usedFor === o ? 'selected' : ''}>${o}</option>`).join('')}
      </select>
      <button class="btn-rm-gram" onclick="removeGramEntry(${i})">✕</button>
    </div>`).join('');
}

function addGramEntry() {
  const id = Number($('bagModalId').value);
  if (!bagMeta[id]) bagMeta[id] = {};
  if (!bagMeta[id].gramEntries) bagMeta[id].gramEntries = [];
  bagMeta[id].gramEntries.push({ date: new Date().toISOString().slice(0, 10), grams: '', usedFor: '' });
  renderGramEntries(id);
  _syncShelf(id);
}

function removeGramEntry(idx) {
  const id = Number($('bagModalId').value);
  if (!bagMeta[id]?.gramEntries) return;
  bagMeta[id].gramEntries.splice(idx, 1);
  renderGramEntries(id);
  _syncShelf(id);
}

function updateGramEntry(idx, field, val) {
  const id = Number($('bagModalId').value);
  if (!bagMeta[id]?.gramEntries?.[idx] === undefined) return;
  bagMeta[id].gramEntries[idx][field] = field === 'grams' ? Number(val) || '' : val;
  _updateGramUI(id);
  _syncShelf(id);
}

async function saveBagMeta() {
  const id = Number($('bagModalId').value);
  if (!bagMeta[id]) bagMeta[id] = {};
  bagMeta[id].roastDate = $('bm-roastDate').value;
  bagMeta[id].deliveredDate = $('bm-deliveredDate').value;
  bagMeta[id].openedDate = $('bm-openedDate').value;
  bagMeta[id].finishedDate = $('bm-finishedDate').value;
  const restVal = parseInt($('bm-restDays').value);
  bagMeta[id].restDays = isNaN(restVal) ? null : restVal;
  // Sync finished state
  if (bagMeta[id].finishedDate) {
    bagMeta[id].isFinished = true;
    if (!finishedBags.includes(id)) finishedBags.push(id);
  } else {
    bagMeta[id].isFinished = false;
    finishedBags = finishedBags.filter(x => x !== id);
  }
  // Persist to D1
  await _syncShelf(id);
  $('bagModalOverlay').classList.add('hidden');
  document.body.style.overflow = '';
  renderShelf();
}

/* ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   RENDER: PROCESS
──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────── */
function detectProcess(l) {
  if (l.category !== 'beans') return null;
  // 1. Prefer the explicit process field
  const explicit = (l.process || '').trim();
  if (explicit) {
    // See if it matches a canonical process
    const match = PROCESS_MAP.find(p =>
      p.key === explicit.toLowerCase() ||
      p.label.toLowerCase() === explicit.toLowerCase()
    );
    if (match) return match;
    // Otherwise it's a custom process — return a synthetic entry
    return { key: '__custom__:' + explicit, label: explicit, icon: '✨', custom: true };
  }
  // 2. Fallback: keyword scan of name/notes
  const haystack = [(l.name || ''), (l.notes || '')].join(' ').toLowerCase();
  for (const p of PROCESS_MAP) {
    if (haystack.includes(p.key)) return p;
  }
  return null;
}

function getBrew(l) {
  try { return JSON.parse(l.brew_equip || '[]') || []; }
  catch { return typeof l.brew_equip === 'string' ? l.brew_equip.split(',').filter(Boolean) : []; }
}

function renderProcess() {
  const beans = logs.filter(l => l.category === 'beans');
  const grouped = {};
  PROCESS_MAP.forEach(p => { grouped[p.key] = []; });
  grouped['unknown'] = [];
  // custom keys will be added dynamically

  beans.forEach(l => {
    const p = detectProcess(l);
    const key = p ? p.key : 'unknown';
    if (!grouped[key]) grouped[key] = [];
    grouped[key].push(l);
  });

  // Build ordered list: canonical processes first, then custom, then unknown
  const customKeys = Object.keys(grouped).filter(k => k.startsWith('__custom__:'));
  const processOrder = [...PROCESS_MAP.map(p => p.key), ...customKeys, 'unknown'];
  let html = '';
  processOrder.forEach(key => {
    const list = grouped[key];
    if (!list || !list.length) return;
    let meta;
    if (key.startsWith('__custom__:')) {
      const label = key.replace('__custom__:', '');
      meta = { label, icon: '✨', hint: 'custom process' };
    } else {
      meta = PROCESS_MAP.find(p => p.key === key) || { label: 'Untagged', icon: '❓', hint: 'process not identified' };
    }
    const total = list.reduce((s, l) => s + Number(l.price || 0), 0);
    html += `<div class="process-group">
      <div class="process-header">
        <span class="process-icon">${meta.icon}</span>
        <div class="process-title-wrap">
          <div class="process-name">${meta.label}</div>
          <div class="process-count">${list.length} bag${list.length !== 1 ? 's' : ''} · ${fmt(total)}</div>
        </div>
      </div>`;
    list.sort((a, b) => new Date(b.date) - new Date(a.date)).forEach(l => {
      const brew = getBrew(l);
      html += `<div class="process-card">
        <div style="display:flex;align-items:flex-start;justify-content:space-between;gap:.7rem">
          <div>
            <div class="process-card-name">${esc(l.name)}</div>
            <div class="process-card-meta">
              <span class="process-card-roaster">${esc(l.vendor)}${l.roaster && l.roaster !== l.vendor ? ' · ' + esc(l.roaster) : ''}</span>
              <span class="process-card-date">${fmtDate(l.date)}</span>
              ${l.size ? `<span class="epill">${esc(l.size)}</span>` : ''}
              ${l.coffee_type && TYPEMETA[l.coffee_type] ? `<span class="epill">${TYPEMETA[l.coffee_type].label}</span>` : ''}
            </div>
            ${brew.length ? `<div class="entry-pills" style="margin-top:.38rem">${brew.map(b => `<span class="epill brew">☕ ${esc(b)}</span>`).join('')}</div>` : ''}
            ${l.notes ? `<div class="entry-notes" style="margin-top:.28rem">${esc(l.notes)}</div>` : ''}
          </div>

        </div>
      </div>`;
    });
    html += '</div>';
  });

  $('processList').innerHTML = html || '<div class="empty-state"><div class="icon">🌿</div><p>Log some beans to see them by process.</p></div>';
}

/* ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   RENDER: JOURNAL
──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────── */
function renderJournal() {
  // stats
  const total = journal.length;
  const rated = journal.filter(j => j.rating > 0);
  const avgR = rated.length ? Math.round(rated.reduce((s, j) => s + j.rating, 0) / rated.length * 10) / 10 : null;
  const brewCount = {};
  journal.forEach(j => { if (j.brewer) brewCount[j.brewer] = (brewCount[j.brewer] || 0) + 1; });
  const favBrew = Object.entries(brewCount).sort((a, b) => b[1] - a[1])[0]?.[0] || '—';
  $('jsTotal').textContent = total;
  $('jsAvgRating').textContent = avgR ? avgR + '★' : '—';
  $('jsFavMethod').textContent = favBrew.split(' ')[0]; // short label
  // Money saved stat in journal header (journal-tracked sessions only)
  const { totalSaved } = calcMoneySaved();
  $('jsSaved').textContent = (totalSaved >= 0 ? '₹' : '-₹') + Math.abs(totalSaved).toLocaleString('en-IN', { maximumFractionDigits: 0 });

  if (!journal.length) {
    $('journalList').innerHTML = '<div class="empty-state"><div class="icon">📓</div><p>Log your first brew session!</p></div>';
    return;
  }

  const sorted = [...journal].sort((a, b) => new Date(b.date) - new Date(a.date));
  $('journalList').innerHTML = sorted.map(j => {
    const stars = '★'.repeat(j.rating || 0) + '☆'.repeat(5 - (j.rating || 0));
    const tastes = (j.tastes || []).map(t => `<span class="taste-chip">${esc(t)}</span>`).join('');
    const meta = [];
    if (j.brewer) meta.push(`<span class="epill brew">☕ ${esc(j.brewer)}</span>`);
    if (j.is_milk_based) meta.push(`<span class="epill" style="color:var(--caramel);font-weight:600">🥛 Milk-based</span>`);
    if (j.dose) meta.push(`<span class="epill">${j.dose}g dose</span>`);
    if (j.yield) meta.push(`<span class="epill">${j.yield}g yield</span>`);
    if (j.time) meta.push(`<span class="epill">${j.time}s</span>`);
    if (j.temp) meta.push(`<span class="epill">${j.temp}°C</span>`);
    if (j.grinder) meta.push(`<span class="epill">⚙️ ${esc(j.grinder)}</span>`);
    if (j.grind) meta.push(`<span class="epill">ðŸ“ ${esc(j.grind)}</span>`);
    const beanName = j.beanId ? logs.find(l => l.id === parseInt(j.beanId))?.name || j.beanLabel || '' : j.beanLabel || '';
    return `<div class="journal-entry">
      <div class="je-header">
        <div>
          <div class="je-title">${beanName ? esc(beanName) : 'Brew Session'}</div>
          <div style="font-size:.72rem;color:var(--caramel);margin-top:.1rem">${stars}</div>
        </div>
        <div class="je-date">${fmtDate(j.date)}</div>
      </div>
      ${meta.length ? `<div class="je-meta">${meta.join('')}</div>` : ''}
      ${tastes ? `<div class="tasting-chips" style="margin-top:.4rem">${tastes}</div>` : ''}
      ${j.notes ? `<div class="je-notes">${esc(j.notes)}</div>` : ''}
      <div class="je-actions">
        <button class="action-btn share-btn" onclick="shareJournalEntry(${j.id})">↗ Share</button>
        <button class="action-btn" onclick="openJournalModal(${j.id})">Edit</button>
        <button class="action-btn del" onclick="deleteJournalEntry(${j.id})">✕</button>
      </div>
    </div>`;
  }).join('');
}

function openJournalModal(editId) {
  // populate bean select from logs — exclude finished bags
  const beans = logs.filter(l => l.category === 'beans' && !finishedBags.includes(l.id)).sort((a, b) => new Date(b.date) - new Date(a.date));
  $('j-bean').innerHTML = '<option value="">— Select a bean —</option>' +
    beans.map(b => `<option value="${b.id}">${esc(b.name)} (${esc(b.roaster || b.vendor)})</option>`).join('');

  // populate brewers and grinders
  populateBrewerGrinderDropdowns();

  if (editId) {
    const j = journal.find(x => String(x.id) === String(editId));
    if (!j) return;
    jEditId = editId;
    $('jModalTitle').textContent = 'Edit Session';
    $('j-date').value = j.date || '';

    // Set Brewer selection
    const brewerVal = j.brewer || '';
    const hasBrewerOption = [...$('j-brewer').options].some(opt => opt.value === brewerVal);
    if (hasBrewerOption) {
      $('j-brewer').value = brewerVal;
      toggleCustomBrewerField();
    } else if (brewerVal) {
      $('j-brewer').value = 'other';
      toggleCustomBrewerField();
      $('j-brewer-custom').value = brewerVal;
    } else {
      $('j-brewer').value = '';
      toggleCustomBrewerField();
    }

    // Set Grinder selection
    const grinderVal = j.grinder || '';
    const hasGrinderOption = [...$('j-grinder').options].some(opt => opt.value === grinderVal);
    if (hasGrinderOption) {
      $('j-grinder').value = grinderVal;
      onGrinderChange();
    } else if (grinderVal) {
      $('j-grinder').value = 'other';
      onGrinderChange();
      $('j-grinder-custom').value = grinderVal;
    } else {
      $('j-grinder').value = '';
      onGrinderChange();
    }

    $('j-bean').value = j.beanId || '';
    $('j-dose').value = j.dose || '';
    $('j-yield').value = j.yield || '';
    $('j-time').value = j.time || '';
    $('j-temp').value = j.temp || '';
    $('j-grind').value = j.grind || '';
    $('j-notes').value = j.notes || '';
    if ($('j-milk')) $('j-milk').checked = !!j.is_milk_based;
    setRating(j.rating || 0);
    document.querySelectorAll('#tastePills .pill-opt').forEach(p => {
      p.classList.toggle('selected', (j.tastes || []).includes(p.textContent));
    });

    // Auto-open accordions if they contain data
    const grinderEl = $('j-grinder');
    if (grinderEl) {
      const advancedDetails = grinderEl.closest('details');
      if (j.grinder || j.dose || j.yield || j.time || j.temp || j.grind) {
        if (advancedDetails) advancedDetails.open = true;
      } else {
        if (advancedDetails) advancedDetails.open = false;
      }
    }

    const tastePillsEl = $('tastePills');
    if (tastePillsEl) {
      const tastesDetails = tastePillsEl.closest('details');
      if (tastesDetails) tastesDetails.open = (j.tastes && j.tastes.length > 0);
    }

  } else {
    jEditId = null; jRating = 0;
    $('jModalTitle').textContent = 'New Brew Session';
    $('j-date').valueAsDate = new Date();
    $('j-brewer').value = '';
    toggleCustomBrewerField();
    $('j-grinder').value = '';
    onGrinderChange();
    $('j-bean').value = '';
    ['j-dose', 'j-yield', 'j-time', 'j-temp', 'j-grind', 'j-notes'].forEach(id => $(id).value = '');
    if ($('j-milk')) $('j-milk').checked = false;
    document.querySelectorAll('#jRatingStars .star').forEach(s => s.classList.remove('filled'));
    document.querySelectorAll('#tastePills .pill-opt').forEach(p => p.classList.remove('selected'));

    // Ensure accordions are closed for a new session
    const grinderEl = $('j-grinder');
    if (grinderEl) {
      const advancedDetails = grinderEl.closest('details');
      if (advancedDetails) advancedDetails.open = false;
    }
    const tastePillsEl = $('tastePills');
    if (tastePillsEl) {
      const tastesDetails = tastePillsEl.closest('details');
      if (tastesDetails) tastesDetails.open = false;
    }
  }
  $('journalModal').classList.add('open');
  document.body.style.overflow = 'hidden';
}

function closeJModal() {
  $('journalModal').classList.remove('open');
  document.body.style.overflow = '';
}
function maybeCloseJModal(e) { if (e.target === $('journalModal')) closeJModal(); }

function setRating(v) {
  jRating = v;
  document.querySelectorAll('#jRatingStars .star').forEach(s => {
    s.classList.toggle('filled', parseInt(s.dataset.v) <= v);
  });
}

async function saveJournalEntry() {
  const date = $('j-date').value;

  let brewer = $('j-brewer').value;
  if (brewer === 'other') {
    const customVal = ($('j-brewer-custom').value || '').trim();
    if (!customVal) { alert('Please enter a custom brewer name.'); return; }
    brewer = customVal;
    if ($('j-brewer-save').checked) {
      const customs = _loadCustomBrewers();
      if (!customs.includes(brewer)) {
        customs.push(brewer);
        _saveCustomBrewers(customs);
      }
    }
  }

  let grinder = $('j-grinder').value;
  if (grinder === 'other') {
    const customVal = ($('j-grinder-custom').value || '').trim();
    if (!customVal) { alert('Please enter a custom grinder name.'); return; }
    grinder = customVal;
    if ($('j-grinder-save').checked) {
      const customs = _loadCustomGrinders();
      if (!customs.includes(grinder)) {
        customs.push(grinder);
        _saveCustomGrinders(customs);
      }
    }
  }

  const beanId = $('j-bean').value;
  const beanLabel = beanId ? $('j-bean').options[$('j-bean').selectedIndex].text : '';
  const dose = parseFloat($('j-dose').value) || 0;
  const yld = parseFloat($('j-yield').value) || 0;
  const time = parseFloat($('j-time').value) || 0;
  const temp = parseFloat($('j-temp').value) || 0;
  const grind = $('j-grind').value.trim();
  const notes = $('j-notes').value.trim();
  const tastes = [...document.querySelectorAll('#tastePills .multi.selected')].map(p => p.textContent);
  const is_milk_based = $('j-milk') ? ($('j-milk').checked ? 1 : 0) : 0;

  const entry = {
    id: jEditId || Date.now(),
    date, brewer, grinder, beanId, beanLabel, dose, yield: yld, time, temp, grind, notes,
    rating: jRating, tastes, is_milk_based
  };

  // Sync to D1
  if (jEditId) {
    await fetch(API + '?route=journal', {
      method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(entry)
    });
  } else {
    await fetch(API + '?route=journal', {
      method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(entry)
    });
  }
  // Reload from D1 so all devices stay in sync
  const jr = await fetch(API + '?route=journal');
  const jd = await jr.json();
  journal = (jd.journal || []).map(j => ({
    ...j,
    beanId: j.beanId || j.bean_id,
    beanLabel: j.beanLabel || j.bean_label,
    tastes: typeof j.tastes === 'string' ? JSON.parse(j.tastes || '[]') : (j.tastes || []),
    is_milk_based: j.is_milk_based || 0,
  }));
  closeJModal();
  renderJournal();
}

async function deleteJournalEntry(id) {
  if (!confirm('Delete this session?')) return;
  await fetch(API, {
    method: 'POST', headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ action: 'deleteJournal', id })
  });
  journal = journal.filter(j => String(j.id) !== String(id));
  renderJournal();
}


/* ── SHARE CARD THEMES ─────────────────────────────────────────── */
const SHARE_THEMES = {
  espresso: {
    name: 'Espresso',
    bg1: '#F5EFE0', bg2: '#EDE4CE',
    card: '#FFFDF6', cardBorder: 'rgba(196,125,42,0.22)',
    accent1: '#C47D2A', accent2: '#E8A83E',
    title: '#2C1A0E', sub: '#7A5C3A', muted: '#A68A64',
    star: '#E8A83E', pill: 'rgba(196,125,42,0.12)', pillText: '#7A3A18',
    divider: 'rgba(196,125,42,0.25)', brand: '#C47D2A', brandSub: '#D4B483',
    shadow: 'rgba(44,26,14,0.18)', ring: '#C47D2A',
  },
  midnight: {
    name: 'Midnight',
    bg1: '#0D1B2A', bg2: '#1B2B3A',
    card: '#112032', cardBorder: 'rgba(180,150,80,0.25)',
    accent1: '#B49650', accent2: '#D4B870',
    title: '#F0E8C8', sub: '#A08C60', muted: '#6A7A8A',
    star: '#D4B870', pill: 'rgba(180,150,80,0.15)', pillText: '#D4C080',
    divider: 'rgba(180,150,80,0.2)', brand: '#B49650', brandSub: '#6A7A8A',
    shadow: 'rgba(0,0,0,0.5)', ring: '#2A4060',
  },
  matcha: {
    name: 'Matcha',
    bg1: '#F0F5EC', bg2: '#E4EDE0',
    card: '#FAFDF8', cardBorder: 'rgba(80,130,60,0.2)',
    accent1: '#5A8A3C', accent2: '#82B45A',
    title: '#1C3018', sub: '#4A6A38', muted: '#7A9A68',
    star: '#82B45A', pill: 'rgba(80,130,60,0.1)', pillText: '#3A6828',
    divider: 'rgba(80,130,60,0.2)', brand: '#5A8A3C', brandSub: '#82B45A',
    shadow: 'rgba(20,50,10,0.14)', ring: '#5A8A3C',
  },
  sakura: {
    name: 'Sakura',
    bg1: '#FDF0F3', bg2: '#F5E0E8',
    card: '#FFF8FA', cardBorder: 'rgba(200,100,130,0.2)',
    accent1: '#C86482', accent2: '#E8909A',
    title: '#3C1020', sub: '#885060', muted: '#B08090',
    star: '#E8909A', pill: 'rgba(200,100,130,0.1)', pillText: '#9A3050',
    divider: 'rgba(200,100,130,0.2)', brand: '#C86482', brandSub: '#B08090',
    shadow: 'rgba(80,20,40,0.14)', ring: '#C86482',
  },
  slate: {
    name: 'Slate',
    bg1: '#F2F4F6', bg2: '#E8ECF0',
    card: '#FAFBFC', cardBorder: 'rgba(80,110,150,0.2)',
    accent1: '#4A6FA5', accent2: '#6A90C0',
    title: '#1A2535', sub: '#4A5A70', muted: '#7A8A9A',
    star: '#6A90C0', pill: 'rgba(74,111,165,0.1)', pillText: '#2A4878',
    divider: 'rgba(74,111,165,0.2)', brand: '#4A6FA5', brandSub: '#7A8A9A',
    shadow: 'rgba(20,35,60,0.14)', ring: '#4A6FA5',
  },
  obsidian: {
    name: 'Obsidian',
    bg1: '#0A0A0A', bg2: '#141414',
    card: '#1C1C1C', cardBorder: 'rgba(255,255,255,0.1)',
    accent1: '#E0E0E0', accent2: '#FFFFFF',
    title: '#F5F5F5', sub: '#A0A0A0', muted: '#606060',
    star: '#E0E0E0', pill: 'rgba(255,255,255,0.08)', pillText: '#D0D0D0',
    divider: 'rgba(255,255,255,0.12)', brand: '#C0C0C0', brandSub: '#606060',
    shadow: 'rgba(0,0,0,0.7)', ring: '#303030',
  },
};

let _shareEntryId = null;
let _shareTheme = 'espresso';

function shareJournalEntry(id) {
  _shareEntryId = id;
  const overlay = $('shareCardOverlay');
  if (overlay) {
    overlay.classList.remove('hidden');
    document.body.style.overflow = 'hidden';

    // Set custom notes text
    const j = journal.find(x => String(x.id) === String(_shareEntryId));
    const notesInput = $('shareCustomNotes');
    if (notesInput) {
      notesInput.value = j ? j.notes || '' : '';
    }

    overlay.querySelectorAll('.share-aspect-btn').forEach(b => b.classList.remove('active'));
    const defaultBtn = overlay.querySelector('.share-aspect-btn[data-aspect="916"]');
    if (defaultBtn) defaultBtn.classList.add('active');
    // Mark active theme swatch
    overlay.querySelectorAll('.theme-swatch-wrapper').forEach(s => {
      s.classList.toggle('active', s.dataset.theme === _shareTheme);
    });
    _renderSharePreview('916');
  }
}

function closeShareOverlay() {
  const overlay = $('shareCardOverlay');
  if (overlay) overlay.classList.add('hidden');
  document.body.style.overflow = '';
  _shareEntryId = null;
}

function maybeCloseShareOverlay(e) {
  if (e.target === $('shareCardOverlay')) closeShareOverlay();
}

function selectShareTheme(el, theme) {
  _shareTheme = theme;
  el.closest('.share-theme-row').querySelectorAll('.theme-swatch-wrapper').forEach(s => s.classList.remove('active'));
  el.classList.add('active');
  const overlay = $('shareCardOverlay');
  const activeAspect = overlay?.querySelector('.share-aspect-btn.active')?.dataset?.aspect || '916';
  _renderSharePreview(activeAspect);
}

function updateShareNotes() {
  const overlay = $('shareCardOverlay');
  const activeAspect = overlay?.querySelector('.share-aspect-btn.active')?.dataset?.aspect || '916';
  _renderSharePreview(activeAspect);
}

function _renderSharePreview(aspect) {
  const canvas = $('sharePreviewCanvas');
  if (!canvas || !_shareEntryId) return;
  const j = journal.find(x => String(x.id) === String(_shareEntryId));
  if (!j) return;
  const beanName = j.beanId ? logs.find(l => l.id === parseInt(j.beanId))?.name || j.beanLabel || '' : j.beanLabel || '';
  _drawJournalCard(canvas, j, beanName, aspect, SHARE_THEMES[_shareTheme] || SHARE_THEMES.espresso);
}

function _drawJournalCard(canvas, j, beanName, aspect, theme) {
  const W = aspect === '916' ? 1080 : 1200;
  const H = aspect === '916' ? 1920 : 900;
  canvas.width = W;
  canvas.height = H;
  const ctx = canvas.getContext('2d');

  // ── Background gradient ──
  const bgGrad = ctx.createLinearGradient(0, 0, W * 0.6, H);
  bgGrad.addColorStop(0, theme.bg1);
  bgGrad.addColorStop(1, theme.bg2);
  ctx.fillStyle = bgGrad;
  ctx.fillRect(0, 0, W, H);

  // ── Geometric accent shapes (top-right corner block) ──
  ctx.save();
  ctx.globalAlpha = 0.08;
  ctx.fillStyle = theme.accent1;
  ctx.fillRect(W * 0.7, 0, W * 0.3, H * 0.22);
  ctx.restore();

  ctx.save();
  ctx.globalAlpha = 0.05;
  ctx.fillStyle = theme.accent2;
  ctx.fillRect(W * 0.82, 0, W * 0.18, H * 0.12);
  ctx.restore();

  // Bottom-left accent strip
  ctx.save();
  ctx.globalAlpha = 0.06;
  ctx.fillStyle = theme.accent1;
  ctx.fillRect(0, H * 0.88, W * 0.25, H * 0.12);
  ctx.restore();

  // ── Card ──
  const PAD = aspect === '916' ? 72 : 60;
  const cardX = PAD;
  const cardW = W - PAD * 2;
  const cardY = aspect === '916' ? H * 0.16 : H * 0.1;
  const cardH = aspect === '916' ? H * 0.71 : H * 0.82;
  const cardRad = 36;

  ctx.save();
  ctx.shadowColor = theme.shadow;
  ctx.shadowBlur = 70;
  ctx.shadowOffsetY = 24;
  _roundRect(ctx, cardX, cardY, cardW, cardH, cardRad);
  ctx.fillStyle = theme.card;
  ctx.fill();
  ctx.restore();

  // Card border
  ctx.save();
  _roundRect(ctx, cardX, cardY, cardW, cardH, cardRad);
  ctx.strokeStyle = theme.cardBorder;
  ctx.lineWidth = 1.5;
  ctx.stroke();
  ctx.restore();

  // Top accent bar across full card width
  ctx.save();
  const topBarH = aspect === '916' ? 10 : 8;
  _roundRect(ctx, cardX, cardY, cardW, topBarH, [cardRad, cardRad, 0, 0]);
  const topBarGrad = ctx.createLinearGradient(cardX, 0, cardX + cardW, 0);
  topBarGrad.addColorStop(0, theme.accent1);
  topBarGrad.addColorStop(1, theme.accent2);
  ctx.fillStyle = topBarGrad;
  ctx.fill();
  ctx.restore();

  // ── Content ──
  const cPad = aspect === '916' ? 68 : 56;
  const cX = cardX + cPad;
  const cW = cardW - cPad * 2;
  let cY = cardY + (aspect === '916' ? 80 : 62);

  // LABEL "BREW LOG"
  ctx.font = aspect === '916'
    ? `500 26px "Helvetica Neue", Arial, sans-serif`
    : `500 22px "Helvetica Neue", Arial, sans-serif`;
  ctx.fillStyle = theme.accent1;
  ctx.textAlign = 'left';
  ctx.letterSpacing = '0.15em';
  ctx.fillText('BREW LOG', cX, cY);
  ctx.letterSpacing = '0';
  cY += aspect === '916' ? 56 : 44;

  // Get roaster and process if available
  let roasterName = '';
  let processName = '';
  if (j.beanId) {
    const beanLog = logs.find(l => l.id === parseInt(j.beanId));
    if (beanLog) {
      roasterName = beanLog.roaster || beanLog.vendor || '';
      const proc = detectProcess(beanLog);
      if (proc) processName = proc.label;
    }
  }

  // Title (bean name)
  const title = beanName || 'Brew Session';
  const titleFs = aspect === '916' ? 68 : 56;
  ctx.font = `bold ${titleFs}px Georgia, "Times New Roman", serif`;
  ctx.fillStyle = theme.title;
  ctx.textAlign = 'left';
  cY = _wrapText(ctx, title, cX, cY, cW, aspect === '916' ? 80 : 66);

  // Draw roaster & process if exists
  let subText = '';
  if (roasterName) subText += "by " + roasterName;
  if (processName) {
    if (subText) subText += "  •  ";
    subText += processName;
  }

  if (subText) {
    // Tuck the subtitle closer under the large title font
    cY -= aspect === '916' ? 20 : 16;
    const subFs = aspect === '916' ? 32 : 28;
    ctx.font = `italic ${subFs}px Georgia, serif`;
    ctx.fillStyle = theme.sub;
    cY = _wrapText(ctx, subText, cX, cY, cW, aspect === '916' ? 42 : 36);
    // Add balanced space before the stars
    cY += aspect === '916' ? 36 : 28;
  } else {
    // Default spacing if no roaster
    cY += aspect === '916' ? 24 : 18;
  }

  // Stars + date on same row
  const rating = j.rating || 0;
  const starStr = '★'.repeat(rating) + '☆'.repeat(5 - rating);
  const starFs = aspect === '916' ? 46 : 38;
  ctx.font = `${starFs}px serif`;
  ctx.fillStyle = theme.star;
  ctx.fillText(starStr, cX, cY);

  const dateStr = j.date
    ? new Date(j.date + 'T00:00:00').toLocaleDateString('en-IN', { day: '2-digit', month: 'short', year: 'numeric' })
    : '';
  const dateFs = aspect === '916' ? 30 : 25;
  ctx.font = `${dateFs}px "Courier New", monospace`;
  ctx.fillStyle = theme.muted;
  ctx.textAlign = 'right';
  ctx.fillText(dateStr, cX + cW, cY);
  ctx.textAlign = 'left';
  cY += aspect === '916' ? 56 : 46;

  // ── Solid divider ──
  ctx.save();
  ctx.strokeStyle = theme.divider;
  ctx.lineWidth = 1;
  ctx.setLineDash([]);
  ctx.beginPath();
  ctx.moveTo(cX, cY);
  ctx.lineTo(cX + cW, cY);
  ctx.stroke();
  ctx.restore();
  cY += aspect === '916' ? 42 : 32;

  // ── Brew params: 3-column layout ──
  const params = [];
  if (j.brewer)  params.push({ label: 'BREWER',  val: j.brewer });
  if (j.grinder) params.push({ label: 'GRINDER', val: j.grinder });
  if (j.dose)    params.push({ label: 'DOSE',    val: j.dose + 'g' });
  if (j.yield)   params.push({ label: 'YIELD',   val: j.yield + 'g' });
  if (j.time)    params.push({ label: 'TIME',    val: j.time + 's' });
  if (j.temp)    params.push({ label: 'TEMP',    val: j.temp + '°C' });
  if (j.grind)   params.push({ label: 'GRIND',   val: j.grind });

  if (params.length) {
    const cols = 3;
    const cellW = cW / cols;
    const labelFs = aspect === '916' ? 24 : 20;
    const valFs = aspect === '916' ? 34 : 28;
    const rowH = aspect === '916' ? 84 : 68;
    params.slice(0, 6).forEach((p, i) => {
      const col = i % cols;
      const row = Math.floor(i / cols);
      const px = cX + col * cellW;
      const py = cY + row * rowH;
      ctx.font = `500 ${labelFs}px "Helvetica Neue", Arial, sans-serif`;
      ctx.fillStyle = theme.muted;
      ctx.letterSpacing = '0.1em';
      ctx.fillText(p.label, px, py);
      ctx.letterSpacing = '0';
      ctx.font = `bold ${valFs}px "Helvetica Neue", Arial, sans-serif`;
      ctx.fillStyle = theme.title;
      ctx.fillText(p.val, px, py + valFs + 4, cellW - 20); // prevent overlap
    });
    cY += Math.ceil(Math.min(params.length, 6) / cols) * rowH + (aspect === '916' ? 16 : 12);
  }

  // ── Tasting notes pills ──
  const tastes = j.tastes || [];
  if (tastes.length) {
    ctx.save();
    ctx.strokeStyle = theme.divider;
    ctx.lineWidth = 1;
    ctx.setLineDash([]);
    ctx.beginPath();
    ctx.moveTo(cX, cY);
    ctx.lineTo(cX + cW, cY);
    ctx.stroke();
    ctx.restore();
    cY += aspect === '916' ? 36 : 26;

    const labelFs2 = aspect === '916' ? 24 : 20;
    ctx.font = `500 ${labelFs2}px "Helvetica Neue", Arial, sans-serif`;
    ctx.fillStyle = theme.muted;
    ctx.letterSpacing = '0.1em';
    ctx.fillText('TASTING NOTES', cX, cY);
    ctx.letterSpacing = '0';
    cY += aspect === '916' ? 42 : 32;

    const pillH = aspect === '916' ? 50 : 40;
    const pillFs = aspect === '916' ? 26 : 22;
    const pillPad = aspect === '916' ? 26 : 20;
    const pillGap = 12;
    ctx.font = `${pillFs}px "Helvetica Neue", Arial, sans-serif`;
    let px2 = cX, py2 = cY;
    tastes.slice(0, 10).forEach(t => {
      const tw = ctx.measureText(t).width;
      const pillW = tw + pillPad * 2;
      if (px2 + pillW > cX + cW) { px2 = cX; py2 += pillH + 10; }
      ctx.save();
      _roundRect(ctx, px2, py2 - pillH + 10, pillW, pillH, pillH / 2);
      ctx.fillStyle = theme.pill;
      ctx.fill();
      ctx.restore();
      ctx.fillStyle = theme.pillText;
      ctx.fillText(t, px2 + pillPad, py2);
      px2 += pillW + pillGap;
    });
    cY = py2 + pillH + (aspect === '916' ? 10 : 6);
  }

  // ── Notes ──
  const customNotesEl = $('shareCustomNotes');
  const noteText = customNotesEl ? customNotesEl.value.trim() : (j.notes || '');

  if (noteText && cY < cardY + cardH - (aspect === '916' ? 160 : 120)) {
    ctx.save();
    ctx.strokeStyle = theme.divider;
    ctx.lineWidth = 1;
    ctx.beginPath();
    ctx.moveTo(cX, cY);
    ctx.lineTo(cX + cW, cY);
    ctx.stroke();
    ctx.restore();
    cY += aspect === '916' ? 36 : 26;
    ctx.font = `italic ${aspect === '916' ? '32px' : '26px'} Georgia, serif`;
    ctx.fillStyle = theme.sub;
    cY = _wrapText(ctx, '\u201c' + noteText + '\u201d', cX, cY, cW, aspect === '916' ? 44 : 36);
  }

  // ── Branding footer ──
  const footY = aspect === '916' ? H * 0.88 : H * 0.89;
  // Accent line above brand
  ctx.save();
  const lineGrad = ctx.createLinearGradient(W / 2 - 120, 0, W / 2 + 120, 0);
  lineGrad.addColorStop(0, 'transparent');
  lineGrad.addColorStop(0.3, theme.accent1);
  lineGrad.addColorStop(0.7, theme.accent2);
  lineGrad.addColorStop(1, 'transparent');
  ctx.strokeStyle = lineGrad;
  ctx.lineWidth = 1.5;
  ctx.beginPath();
  ctx.moveTo(W / 2 - 120, footY - (aspect === '916' ? 28 : 22));
  ctx.lineTo(W / 2 + 120, footY - (aspect === '916' ? 28 : 22));
  ctx.stroke();
  ctx.restore();

  ctx.font = aspect === '916'
    ? `bold 34px "Helvetica Neue", Arial, sans-serif`
    : `bold 28px "Helvetica Neue", Arial, sans-serif`;
  ctx.textAlign = 'center';
  ctx.fillStyle = theme.brand;
  ctx.letterSpacing = '0.06em';
  ctx.fillText("GAURAV'S COFFEE LAB", W / 2, footY);
  ctx.letterSpacing = '0';

  // ── Scale canvas preview ──
  const preview = $('sharePreviewCanvas');
  if (preview) {
    const wrap = $('sharePreviewWrap');
    if (wrap) {
      const maxW = Math.min(wrap.clientWidth - 32, aspect === '916' ? 270 : 410);
      const scale = maxW / W;
      preview.style.width  = Math.round(W * scale) + 'px';
      preview.style.height = Math.round(H * scale) + 'px';
    }
  }
}

function _roundRect(ctx, x, y, w, h, r) {
  const radii = Array.isArray(r) ? r : [r, r, r, r];
  ctx.beginPath();
  ctx.moveTo(x + radii[0], y);
  ctx.lineTo(x + w - radii[1], y);
  ctx.quadraticCurveTo(x + w, y, x + w, y + radii[1]);
  ctx.lineTo(x + w, y + h - radii[2]);
  ctx.quadraticCurveTo(x + w, y + h, x + w - radii[2], y + h);
  ctx.lineTo(x + radii[3], y + h);
  ctx.quadraticCurveTo(x, y + h, x, y + h - radii[3]);
  ctx.lineTo(x, y + radii[0]);
  ctx.quadraticCurveTo(x, y, x + radii[0], y);
  ctx.closePath();
}

function _wrapText(ctx, text, x, y, maxW, lineH) {
  const words = text.split(' ');
  let line = '';
  for (let i = 0; i < words.length; i++) {
    const test = line + words[i] + ' ';
    if (ctx.measureText(test).width > maxW && i > 0) {
      ctx.fillText(line.trim(), x, y);
      line = words[i] + ' ';
      y += lineH;
    } else {
      line = test;
    }
  }
  ctx.fillText(line.trim(), x, y);
  return y + lineH;
}

function selectShareAspect(btn, aspect) {
  btn.closest('.share-aspect-group').querySelectorAll('.share-aspect-btn').forEach(b => b.classList.remove('active'));
  btn.classList.add('active');
  _renderSharePreview(aspect);
}

function downloadShareCard() {
  const canvas = $('sharePreviewCanvas');
  if (!canvas) return;
  const overlay = $('shareCardOverlay');
  const activeBtn = overlay && overlay.querySelector('.share-aspect-btn.active');
  const aspect = activeBtn ? activeBtn.dataset.aspect : '916';
  const j = journal.find(x => String(x.id) === String(_shareEntryId));
  const name = (j?.beanLabel || j?.brewer || 'brew').toLowerCase().replace(/[^a-z0-9]/g, '-');
  const filename = `gcl-${name}-${aspect === '916' ? '9x16' : '4x3'}.jpg`;
  const link = document.createElement('a');
  link.download = filename;
  link.href = canvas.toDataURL('image/jpeg', 0.95);
  link.click();
}


/* ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   MONEY SAVED CALCULATOR
──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────── */
// Brewer category classification
const V60_BREWERS = ['V60', 'Pour Over', 'Chemex'];
const ESPRESSO_BREWERS = ['Espresso Machine'];
const KOMPRESSO_BREWERS = ['Cafflano Kompresso'];
const FRENCH_KALDI_BREWERS = ['French Press', 'KaldiPress', 'AeroPress'];
const AMERICANO_BREWERS = ['Americano'];
const COLD_BREWERS = ['Cold Brew'];
const MOKA_POT_BREWERS = ['Moka Pot'];
// Market prices per cup
const MARKET_V60 = 200;        // ₹/cup (V60 / Pour Over)
const MARKET_ESPRESSO = 200;   // ₹/cup
const MARKET_KOMPRESSO = 175;  // ₹/cup
const MARKET_COLD_BREW = 200;  // ₹/cup (Cold Brew)
const MARKET_FRENCH_KALDI = 150; // ₹/cup (French Press, KaldiPress & AeroPress)
const MARKET_AMERICANO = 210;  // ₹/cup (Americano = Espresso + ₹10)
const MARKET_MOKA_POT = 100;   // ₹/cup (Moka Pot)
const MARKET_MILK_BASED = 250; // ₹/cup (milk-based drinks: lattes, cappuccinos, etc.)
const MARKET_STANDARD = 150;   // ₹/cup (all other methods & base price for untracked bags)
const BREW_RATIO = 15;         // 1:15 bean-to-yield ratio (g beans : ml yield)
const STD_CUP_ML = 250;       // ml per standard cup serving

// Parse a bag size string to grams, supporting g, kg, mg units
function parseSizeToGrams(size) {
  if (!size) return 0;
  const s = String(size);
  const kg = s.match(/(\d+(?:\.\d+)?)\s*kg/i);
  if (kg) return Number(kg[1]) * 1000;
  const g = s.match(/(\d+(?:\.\d+)?)\s*g(?!r)/i);
  if (g) return Number(g[1]);
  const mg = s.match(/(\d+(?:\.\d+)?)\s*mg/i);
  if (mg) return Number(mg[1]) / 1000;
  // volume-based: L or ml (treat as ml, assume density ~1g/ml for convenience)
  const l = s.match(/(\d+(?:\.\d+)?)\s*l(?!b)/i);
  if (l) return Number(l[1]) * 1000;
  const ml = s.match(/(\d+(?:\.\d+)?)\s*ml/i);
  if (ml) return Number(ml[1]);
  return 0;
}

// Estimate cups from bean grams using 1:15 ratio and 250ml/cup serving
function gramsToEstimatedCups(grams) {
  const yieldMl = grams * BREW_RATIO;
  return Math.max(1, Math.round(yieldMl / STD_CUP_ML));
}

function marketPriceForBrewer(brewer, isMilkBased) {
  if (isMilkBased) return MARKET_MILK_BASED;
  if (!brewer) return MARKET_STANDARD;
  const b = brewer.toLowerCase();

  if (b.includes('v60') || b.includes('pour over') || b.includes('chemex')) return MARKET_V60;
  if (b.includes('kompresso')) return MARKET_KOMPRESSO;
  if (b.includes('espresso')) return MARKET_ESPRESSO;
  if (b.includes('cold brew')) return MARKET_COLD_BREW;
  if (b.includes('french press') || b.includes('kaldipress') || b.includes('aeropress')) return MARKET_FRENCH_KALDI;
  if (b.includes('americano')) return MARKET_AMERICANO;
  if (b.includes('moka pot')) return MARKET_MOKA_POT;

  return MARKET_STANDARD;
}

// Market price for a gram-entry's usedFor label (shelf tracking)
const USED_FOR_PRICE = {
  'Espresso': 200,
  'Americano': 210,
  'Cold Brew': 200,
  'Pour Over': 200,
  'French Press': 150,
  'KaldiPress': 150,
  'AeroPress': 150,
};
function marketPriceForUsedFor(usedFor) {
  return USED_FOR_PRICE[usedFor] || MARKET_STANDARD;
}

// Returns cost-per-gram for a specific bean log entry
function beanCostPerGram(logEntry) {
  if (!logEntry) return null;
  const price = Number(logEntry.price || 0);
  if (!price) return null;
  const sz = String(logEntry.size || '');
  const gMatch = sz.match(/(\d+(?:\.\d+)?)\s*g/i);
  const kgMatch = sz.match(/(\d+(?:\.\d+)?)\s*kg/i);
  const grams = gMatch ? Number(gMatch[1]) : kgMatch ? Number(kgMatch[1]) * 1000 : 0;
  return grams > 0 ? price / grams : null;
}

// Global average cost per gram from all bean purchases with known price+size
function globalCostPerGram() {
  const beans = logs.filter(l => l.category === 'beans' && l.price && l.size);
  let totalG = 0, totalP = 0;
  beans.forEach(l => {
    const cpg = beanCostPerGram(l);
    if (!cpg) return;
    const sz = String(l.size || '');
    const gM = sz.match(/(\d+(?:\.\d+)?)\s*g/i);
    const kgM = sz.match(/(\d+(?:\.\d+)?)\s*kg/i);
    const g = gM ? Number(gM[1]) : kgM ? Number(kgM[1]) * 1000 : 0;
    totalG += g; totalP += Number(l.price || 0);
  });
  return totalG > 0 ? totalP / totalG : 0;
}

function calcMoneySaved() {
  const gcpg = globalCostPerGram(); // global fallback ₹/g
  const rows = []; // { label, cups, homeCost, cafeCost, saved, note }

  const journalGramsByBean = {};

  // ── Part 1: Journal-tracked sessions ──────────────────
  // Always include journal sessions — they are actual brews regardless of bag status.
  journal.forEach(j => {
    const dose = Number(j.dose) || STD_DOSE_G;
    const isMilk = !!j.is_milk_based;
    const marketPrice = marketPriceForBrewer(j.brewer, isMilk);
    const beanLog = j.beanId ? logs.find(l => l.id === parseInt(j.beanId)) : null;
    const cpg = beanLog ? (beanCostPerGram(beanLog) || gcpg) : gcpg;
    const homeCost = cpg > 0 ? dose * cpg : 0;
    const cafeCost = marketPrice;
    const beanName = beanLog?.name || j.beanLabel || 'Unknown bean';
    const brewerLabel = j.brewer || 'Unknown brewer';
    rows.push({
      label: `${beanName} — ${brewerLabel}${isMilk ? ' 🥛' : ''}`,
      date: j.date,
      cups: 1,
      dose,
      homeCost,
      cafeCost,
      saved: cafeCost - homeCost,
      tracked: true,
      note: `${dose}g dose · ₹${cpg > 0 ? (cpg * dose).toFixed(0) : '?'} beans · vs ₹${marketPrice} café${isMilk ? ' (milk)' : ''}`,
    });
    if (beanLog) {
      journalGramsByBean[beanLog.id] = (journalGramsByBean[beanLog.id] || 0) + dose;
    }
  });

  // ── Part 2: Bean bags processing ──────────────────
  const beans = logs.filter(l => l.category === 'beans');
  beans.forEach(l => {
    const meta = bagMeta[l.id] || {};

    // Exclude unopened bags completely (unless they are marked as finished)
    if (!meta.openedDate && !meta.isFinished) return;

    const totalG = parseSizeToGrams(l.size);
    const cpg = beanCostPerGram(l) || gcpg;

    if (!meta.isFinished) {
      // In-progress bag: only add shelf-tracked grams that EXCEED what's already in journal.
      // Don't project the remaining bag capacity.
      const entries = meta.gramEntries || [];
      if (entries.length === 0) return;

      const shelfGrams = entries.reduce((s, e) => s + Number(e.grams || 0), 0);
      const journalGrams = journalGramsByBean[l.id] || 0;
      const unloggedShelfGrams = Math.max(0, shelfGrams - journalGrams);
      if (unloggedShelfGrams < 1) return;

      // Calculate café cost using per-entry brewer prices
      const unloggedEntries = (() => {
        // Distribute unlogged grams across entries proportionally
        let remaining = unloggedShelfGrams;
        let cafeCostTotal = 0;
        for (const e of [...entries].reverse()) {
          const eg = Math.min(remaining, Number(e.grams || 0));
          if (eg <= 0) continue;
          const entrycups = gramsToEstimatedCups(eg);
          cafeCostTotal += entrycups * marketPriceForUsedFor(e.usedFor);
          remaining -= eg;
          if (remaining <= 0) break;
        }
        return cafeCostTotal;
      })();

      const cups = gramsToEstimatedCups(unloggedShelfGrams);
      const yieldMl = Math.round(unloggedShelfGrams * BREW_RATIO);
      const homeCost = unloggedShelfGrams * cpg;
      const cafeCost = unloggedEntries;

      rows.push({
        label: l.name + ' (Shelf Tracked)',
        date: l.date,
        cups,
        dose: unloggedShelfGrams,
        homeCost,
        cafeCost,
        saved: cafeCost - homeCost,
        tracked: false,
        note: `${unloggedShelfGrams}g shelf-tracked (unlogged) · ${yieldMl}ml yield (1:${BREW_RATIO}) · ~${cups} cups`,
      });

    } else {
      // Finished bag: calculate from total logs, handling unlogged portion
      if (!totalG) return;

      const loggedGrams = journalGramsByBean[l.id] || 0;
      const unloggedGrams = Math.max(0, totalG - loggedGrams);

      if (unloggedGrams >= 1) {
        const cups = gramsToEstimatedCups(unloggedGrams);
        const yieldMl = Math.round(unloggedGrams * BREW_RATIO);
        const homeCost = unloggedGrams * cpg;
        // Use per-entry brewer prices for unlogged portion of finished bags
        const cafeCost = (() => {
          let remaining = unloggedGrams;
          let total = 0;
          for (const e of [...(meta.gramEntries || [])].reverse()) {
            const eg = Math.min(remaining, Number(e.grams || 0));
            if (eg <= 0) continue;
            total += gramsToEstimatedCups(eg) * marketPriceForUsedFor(e.usedFor);
            remaining -= eg;
            if (remaining <= 0) break;
          }
          // If no shelf entries tagged, fall back to ₹150/cup
          return total > 0 ? total : cups * MARKET_STANDARD;
        })();

        rows.push({
          label: l.name + (loggedGrams > 0 ? ' (Unlogged portion)' : ''),
          date: l.date,
          cups,
          dose: unloggedGrams,
          homeCost,
          cafeCost,
          saved: cafeCost - homeCost,
          tracked: false,
          note: `${unloggedGrams}g unlogged · ${yieldMl}ml yield (1:${BREW_RATIO}) · ~${cups} cups`,
        });
      }
    }
  });

  const totalHomeCost = rows.reduce((s, r) => s + r.homeCost, 0);
  const totalCafeCost = rows.reduce((s, r) => s + r.cafeCost, 0);
  const totalSaved = totalCafeCost - totalHomeCost;
  return { rows, totalHomeCost, totalCafeCost, totalSaved };
}

/* ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   RENDER: INSIGHTS
──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────── */
let brewsChartInst = null;
let prepChartInst = null;
let grinderChartInst = null;

function switchStatsTab(tabId) {
  document.querySelectorAll('.stats-tab').forEach(t => t.classList.remove('active'));
  document.querySelectorAll('.stats-pane').forEach(p => p.classList.remove('active'));

  const btn = Array.from(document.querySelectorAll('.stats-tab')).find(b => b.getAttribute('onclick').includes(tabId));
  if (btn) btn.classList.add('active');

  const pane = document.getElementById('tab-' + tabId);
  if (pane) pane.classList.add('active');
}

function renderInsights() {
  const beans = logs.filter(l => l.category === 'beans');
  const gears = logs.filter(l => l.category === 'gears');
  const acc = logs.filter(l => l.category === 'accessories');
  const bTotal = beans.reduce((s, l) => s + Number(l.price || 0), 0);
  const gTotal = gears.reduce((s, l) => s + Number(l.price || 0), 0);
  const aTotal = acc.reduce((s, l) => s + Number(l.price || 0), 0);
  const total = bTotal + gTotal + aTotal;

  const isDark = document.body.classList.contains('dark-mode');
  const C = {
    cream: '#F5EFE0', caramel: '#C47D2A', mid: '#7A5C3A', blue: '#4A6FA5',
    green: '#5E8A3A', parchment: '#EDE4CE', light: '#D4B483', honey: '#E8A83E', purple: '#7A3A8A'
  };
  const gridColor = isDark ? 'rgba(255, 255, 255, 0.1)' : C.parchment;
  const textColor = isDark ? '#A68A64' : C.mid;

  // --- GENERAL TAB ---
  const uniqueGrinders = new Set(journal.map(j => j.grinder).filter(Boolean));
  const uniqueBrewers = new Set(journal.map(j => j.brewer).filter(Boolean));

  let totalBeansBought = 0;
  let totalYieldGrams = 0;

  beans.forEach(l => {
    const sz = String(l.size || '');
    const gM = sz.match(/(\d+(?:\.\d+)?)\s*g/i);
    const kgM = sz.match(/(\d+(?:\.\d+)?)\s*kg/i);
    totalBeansBought += gM ? Number(gM[1]) : kgM ? Number(kgM[1]) * 1000 : 0;
  });

  const journalGramsByBean2 = {};
  journal.forEach(j => {
    const dose = Number(j.dose) || 0;
    totalYieldGrams += Number(j.yield) || (dose * BREW_RATIO);
    if (j.beanId && dose > 0) {
      const bid = parseInt(j.beanId);
      journalGramsByBean2[bid] = (journalGramsByBean2[bid] || 0) + dose;
    }
  });

  // Add unlogged consumption from finished + in-progress bags (1:15 ratio)
  beans.forEach(l => {
    const meta = bagMeta[l.id] || {};
    if (!meta.openedDate && !meta.isFinished) return; // truly unopened, skip

    if (meta.isFinished) {
      // Finished bag: add unlogged portion (total size minus journal-logged grams)
      const totalG = parseSizeToGrams(l.size);
      if (!totalG) return;
      const loggedG = journalGramsByBean2[l.id] || 0;
      const unloggedG = Math.max(0, totalG - loggedG);
      if (unloggedG >= 1) totalYieldGrams += unloggedG * BREW_RATIO;
    } else {
      // In-progress bag: use shelf-tracked grams only
      const entries = meta.gramEntries || [];
      const shelfG = entries.reduce((s, e) => s + Number(e.grams || 0), 0);
      if (shelfG > 0) totalYieldGrams += shelfG * BREW_RATIO;
    }
  });

  const totalConsumedL = totalYieldGrams / 1000;

  $('ins-gen-grinders').textContent = uniqueGrinders.size;
  $('ins-gen-brewers').textContent = uniqueBrewers.size;
  $('ins-gen-sessions').textContent = journal.length;
  $('ins-gen-spend').textContent = fmt(bTotal);
  $('ins-gen-bought').textContent = totalBeansBought.toFixed(0) + 'g';
  $('ins-gen-consumed').textContent = totalConsumedL.toFixed(1) + 'L';

  // Original top logic components
  const roasterTot = {};
  const roasterBags = {};
  beans.forEach(l => {
    const r = l.roaster || l.vendor || 'Unknown';
    roasterTot[r] = (roasterTot[r] || 0) + Number(l.price || 0);
    roasterBags[r] = (roasterBags[r] || 0) + 1;
  });
  const topRoasters = Object.entries(roasterTot).sort((a, b) => b[1] - a[1]);

  // Callouts
  const callouts = [];
  if (bTotal > gTotal + aTotal) callouts.push({ icon: '☕', text: `You spend <strong>${total > 0 ? Math.round(bTotal / total * 100) : 0}%</strong> of your budget on beans — a true coffee purist.` });
  else callouts.push({ icon: '⚙️', text: `You've invested more in gear than beans so far. Once the setup's sorted, let the beans shine.` });
  $('insCallouts').innerHTML = callouts.map(c => `<div class="insight-callout"><span class="callout-icon">${c.icon}</span><div class="callout-text">${c.text}</div></div>`).join('');

  const monthly = {}, monthlyB = {}, monthlyG = {}, monthlyA = {};
  logs.forEach(l => {
    const ym = (l.date || '').slice(0, 7); if (!ym) return;
    const p = Number(l.price || 0);
    monthly[ym] = (monthly[ym] || 0) + p;
    if (l.category === 'beans') monthlyB[ym] = (monthlyB[ym] || 0) + p;
    if (l.category === 'gears') monthlyG[ym] = (monthlyG[ym] || 0) + p;
    if (l.category === 'accessories') monthlyA[ym] = (monthlyA[ym] || 0) + p;
  });
  const sortedM = Object.keys(monthly).sort();
  const mlabels = sortedM.map(m => { const [y, mo] = m.split('-'); return new Date(+y, +mo - 1).toLocaleString('en-IN', { month: 'short', year: '2-digit' }); });

  if (barInst) barInst.destroy();
  barInst = new Chart($('barChart').getContext('2d'), {
    type: 'bar',
    data: {
      labels: mlabels, datasets: [{ label: 'Beans', data: sortedM.map(m => monthlyB[m] || 0), backgroundColor: C.caramel + 'AA', borderColor: C.caramel, borderWidth: 2, borderRadius: 6, stack: 's' },
      { label: 'Gears', data: sortedM.map(m => monthlyG[m] || 0), backgroundColor: C.mid + '99', borderColor: C.mid, borderWidth: 2, borderRadius: 0, stack: 's' },
      { label: 'Accessories', data: sortedM.map(m => monthlyA[m] || 0), backgroundColor: C.blue + '88', borderColor: C.blue, borderWidth: 2, borderRadius: 0, stack: 's' }]
    },
    options: {
      plugins: { legend: { labels: { color: textColor, font: { size: 11, family: 'DM Sans' } } } },
      scales: {
        x: { stacked: true, ticks: { color: textColor, font: { size: 10 } }, grid: { color: gridColor } },
        y: { stacked: true, ticks: { color: textColor, font: { size: 10 }, callback: v => `₹${v.toLocaleString('en-IN')}` }, grid: { color: gridColor } }
      }
    }
  });

  if (pieInst) pieInst.destroy();
  pieInst = new Chart($('pieChart').getContext('2d'), {
    type: 'doughnut',
    data: {
      labels: ['Beans', 'Gears', 'Accessories'], datasets: [{
        data: [bTotal, gTotal, aTotal],
        backgroundColor: [C.caramel, C.mid, C.blue], borderColor: isDark ? '#1A1208' : C.cream, borderWidth: 3
      }]
    },
    options: { plugins: { legend: { labels: { color: textColor, font: { size: 11, family: 'DM Sans' } } } }, cutout: '60%' }
  });

  const typeTot = {};
  beans.forEach(l => { if (l.coffee_type) typeTot[l.coffee_type] = (typeTot[l.coffee_type] || 0) + Number(l.price || 0); });
  const typeE = Object.entries(typeTot).sort((a, b) => b[1] - a[1]);
  const maxT = typeE[0]?.[1] || 1;
  const tCls = ['', 'blue', 'green', 'purple'];
  $('typeBarList').innerHTML = typeE.length ? typeE.map(([k, a], i) =>
    `<div class="bar-row"><span class="bar-label">${TYPEMETA[k]?.label || k}</span><div class="bar-track"><div class="bar-fill ${tCls[i] || ''}" style="width:${(a / maxT * 100).toFixed(1)}%"></div></div><span class="bar-val">${fmt(a)}</span></div>`).join('')
    : '<p style="font-size:.78rem;color:var(--light);font-style:italic">Tag your beans with a type to see this.</p>';

  // --- BREWS TAB ---
  let brewsPerDay = 0;
  if (journal.length > 0) {
    const dates = journal.map(j => new Date(j.date)).sort((a, b) => a - b);
    const spanDays = Math.max(1, Math.floor((new Date() - dates[0]) / 86400000));
    brewsPerDay = journal.length / spanDays;
  }
  $('ins-brw-total').textContent = journal.length;
  $('ins-brw-perday').textContent = brewsPerDay.toFixed(1);
  $('ins-brw-consumed').textContent = totalConsumedL.toFixed(1) + 'L';

  const brewsByMonth = {};
  journal.forEach(j => {
    const ym = j.date.slice(0, 7);
    brewsByMonth[ym] = (brewsByMonth[ym] || 0) + 1;
  });
  const bSortedM = Object.keys(brewsByMonth).sort();

  if (brewsChartInst) brewsChartInst.destroy();
  brewsChartInst = new Chart($('brewsChart').getContext('2d'), {
    type: 'line',
    data: {
      labels: bSortedM.map(m => { const [y, mo] = m.split('-'); return new Date(+y, +mo - 1).toLocaleString('en-IN', { month: 'short', year: '2-digit' }); }),
      datasets: [{ label: 'Brews', data: bSortedM.map(m => brewsByMonth[m]), borderColor: C.caramel, backgroundColor: C.caramel + '22', fill: true, tension: 0.3 }]
    },
    options: {
      plugins: { legend: { display: false } },
      scales: { x: { ticks: { color: textColor }, grid: { color: gridColor } }, y: { ticks: { color: textColor, stepSize: 1 }, grid: { color: gridColor } } }
    }
  });

  // --- BEANS TAB ---
  const rated = journal.filter(j => j.rating > 0);
  const avgIndia = rated.length ? (rated.reduce((s, j) => s + j.rating, 0) / rated.length).toFixed(1) : '0.0';
  $('ins-bean-rating').textContent = avgIndia + '★';

  // Avg ₹/100g and Price/Cup
  const avgCpg = globalCostPerGram(); // ₹/g
  if ($('ins-avg-cpg')) $('ins-avg-cpg').textContent = avgCpg > 0 ? '₹' + (avgCpg * 100).toFixed(0) : '—';
  if ($('ins-price-cup')) $('ins-price-cup').textContent = avgCpg > 0 ? '₹' + (avgCpg * STD_DOSE_G).toFixed(0) : '—';

  const maxV = topRoasters[0]?.[1] || 1;
  $('vendorBarList').innerHTML = topRoasters.slice(0, 8).map(([v, a]) =>
    `<div class="bar-row"><span class="bar-label">${esc(v)}</span><div class="bar-track"><div class="bar-fill" style="width:${(a / maxV * 100).toFixed(1)}%"></div></div><span class="bar-val">${fmt(a)} · ${roasterBags[v] || 0} bag${(roasterBags[v] || 0) !== 1 ? 's' : ''}</span></div>`).join('');

  const procTot = {};
  beans.forEach(l => { const p = detectProcess(l); const k = p ? p.label : 'Untagged'; procTot[k] = (procTot[k] || 0) + Number(l.price || 0); });
  const procE = Object.entries(procTot).sort((a, b) => b[1] - a[1]);
  const maxP = procE[0]?.[1] || 1;
  const pCls = ['', 'green', 'blue', 'purple', 'mid'];
  $('processBarList').innerHTML = procE.length ? procE.map(([k, a], i) =>
    `<div class="bar-row"><span class="bar-label">${esc(k)}</span><div class="bar-track"><div class="bar-fill ${pCls[i] || ''}" style="width:${(a / maxP * 100).toFixed(1)}%"></div></div><span class="bar-val">${fmt(a)}</span></div>`).join('')
    : '<p style="font-size:.78rem;color:var(--light);font-style:italic">Add process info to beans to see this.</p>';

  // --- PREPARATIONS TAB ---
  const methodCount = {};
  const methodHistory = {}; // month -> { method: count }
  journal.forEach(j => {
    if (!j.brewer) return;
    methodCount[j.brewer] = (methodCount[j.brewer] || 0) + 1;
    const ym = j.date.slice(0, 7);
    if (!methodHistory[ym]) methodHistory[ym] = {};
    methodHistory[ym][j.brewer] = (methodHistory[ym][j.brewer] || 0) + 1;
  });

  const mE = Object.entries(methodCount).sort((a, b) => b[1] - a[1]);
  const maxM = mE[0]?.[1] || 1;
  $('brewBarList').innerHTML = mE.length ? mE.map(([k, a], i) =>
    `<div class="bar-row"><span class="bar-label">${esc(k)}</span><div class="bar-track"><div class="bar-fill ${pCls[i] || ''}" style="width:${(a / maxM * 100).toFixed(1)}%"></div></div><span class="bar-val">${a}</span></div>`).join('')
    : '<p style="font-size:.78rem;color:var(--light);font-style:italic">Log brew sessions to see usage.</p>';

  const pSortedM = Object.keys(methodHistory).sort();
  const topMethods = mE.slice(0, 4).map(e => e[0]);
  const pColors = [C.caramel, C.blue, C.green, C.purple];

  if (prepChartInst) prepChartInst.destroy();
  prepChartInst = new Chart($('prepHistoryChart').getContext('2d'), {
    type: 'bar',
    data: {
      labels: pSortedM.map(m => { const [y, mo] = m.split('-'); return new Date(+y, +mo - 1).toLocaleString('en-IN', { month: 'short', year: '2-digit' }); }),
      datasets: topMethods.map((method, i) => ({
        label: method,
        data: pSortedM.map(m => methodHistory[m][method] || 0),
        backgroundColor: pColors[i] + 'AA', borderColor: pColors[i], borderWidth: 1, stack: 's'
      }))
    },
    options: {
      plugins: { legend: { labels: { color: textColor, font: { size: 10 } } } },
      scales: { x: { stacked: true, ticks: { color: textColor }, grid: { color: gridColor } }, y: { stacked: true, ticks: { color: textColor, stepSize: 1 }, grid: { color: gridColor } } }
    }
  });

  // --- GRINDERS TAB ---
  const grinderGround = {}; // grinder -> g
  const grinderHistory = {}; // month -> { grinder: count }
  journal.forEach(j => {
    if (!j.grinder) return;
    grinderGround[j.grinder] = (grinderGround[j.grinder] || 0) + (Number(j.dose) || 0);
    const ym = j.date.slice(0, 7);
    if (!grinderHistory[ym]) grinderHistory[ym] = {};
    grinderHistory[ym][j.grinder] = (grinderHistory[ym][j.grinder] || 0) + 1;
  });

  const gE = Object.entries(grinderGround).sort((a, b) => b[1] - a[1]);
  const maxGr = gE[0]?.[1] || 1;
  $('grinderBarList').innerHTML = gE.length ? gE.map(([k, a], i) =>
    `<div class="bar-row"><span class="bar-label">${esc(k)}</span><div class="bar-track"><div class="bar-fill ${pCls[i] || ''}" style="width:${(a / maxGr * 100).toFixed(1)}%"></div></div><span class="bar-val">${a.toFixed(0)}g</span></div>`).join('')
    : '<p style="font-size:.78rem;color:var(--light);font-style:italic">Log brew sessions to see usage.</p>';

  const gSortedM = Object.keys(grinderHistory).sort();
  const topGrinders = gE.slice(0, 4).map(e => e[0]);
  const gColors = [C.mid, C.caramel, C.blue, C.green];

  if (grinderChartInst) grinderChartInst.destroy();
  grinderChartInst = new Chart($('grinderHistoryChart').getContext('2d'), {
    type: 'bar',
    data: {
      labels: gSortedM.map(m => { const [y, mo] = m.split('-'); return new Date(+y, +mo - 1).toLocaleString('en-IN', { month: 'short', year: '2-digit' }); }),
      datasets: topGrinders.map((gr, i) => ({
        label: gr,
        data: gSortedM.map(m => grinderHistory[m][gr] || 0),
        backgroundColor: gColors[i] + 'AA', borderColor: gColors[i], borderWidth: 1, stack: 's'
      }))
    },
    options: {
      plugins: { legend: { labels: { color: textColor, font: { size: 10 } } } },
      scales: { x: { stacked: true, ticks: { color: textColor }, grid: { color: gridColor } }, y: { stacked: true, ticks: { color: textColor, stepSize: 1 }, grid: { color: gridColor } } }
    }
  });

  // ── Grind Size Recommender ────────────────────────────
  renderGrindRecommender();

  // ── Money Saved section ────────────────────────────────
  renderMoneySaved();
}

function renderGrindRecommender() {
  const el = $('grind-recommender');
  if (!el) return;

  // Target extraction time range for espresso (seconds)
  const ESPRESSO_MIN = 25, ESPRESSO_MAX = 35, ESPRESSO_IDEAL = 28;
  const ESPRESSO_METHODS = ['Espresso Machine', 'Cafflano Kompresso', 'AeroPress'];

  // Collect espresso sessions with a grind setting and brew time
  // group by grinder → grind → array of {time, rating}
  const data = {}; // grinder → grind → { sessions, totalTime, totalRating, ratedCount }
  journal.forEach(j => {
    if (!j.grinder || !j.grind || !j.time) return;
    const isEspressoMethod = ESPRESSO_METHODS.some(m => (j.brewer || '').toLowerCase().includes(m.toLowerCase()));
    if (!isEspressoMethod) return;
    const t = Number(j.time);
    if (!t || t < 5 || t > 120) return; // sanity check
    if (!data[j.grinder]) data[j.grinder] = {};
    if (!data[j.grinder][j.grind]) data[j.grinder][j.grind] = { sessions: 0, totalTime: 0, totalRating: 0, ratedCount: 0 };
    const d = data[j.grinder][j.grind];
    d.sessions++;
    d.totalTime += t;
    if (j.rating > 0) { d.totalRating += j.rating; d.ratedCount++; }
  });

  const grinders = Object.keys(data);
  if (!grinders.length) {
    el.innerHTML = '<p style="font-size:.78rem;color:var(--light);font-style:italic;padding:.5rem 0">Log espresso sessions with grind settings and extraction times to see recommendations.</p>';
    return;
  }

  // For each grinder, rank grind settings by how close avg time is to ideal + avg rating
  let html = '';
  grinders.sort().forEach(grinder => {
    const grinds = Object.entries(data[grinder]).map(([grind, d]) => {
      const avgTime = d.totalTime / d.sessions;
      const avgRating = d.ratedCount > 0 ? d.totalRating / d.ratedCount : null;
      const timeDiff = Math.abs(avgTime - ESPRESSO_IDEAL);
      const inRange = avgTime >= ESPRESSO_MIN && avgTime <= ESPRESSO_MAX;
      // Score: lower timeDiff is better; bonus for high rating
      const score = (inRange ? 100 : 50) - timeDiff + (avgRating ? avgRating * 3 : 0);
      return { grind, avgTime, avgRating, sessions: d.sessions, inRange, score, timeDiff };
    }).sort((a, b) => b.score - a.score);

    const best = grinds[0];
    const extraHtml = grinds.slice(1, 3).map(g => {
      const icon = g.inRange ? '🟡' : (g.avgTime < ESPRESSO_MIN ? '🔵' : '🔴');
      return `<div class="gr-alt-row">${icon} <strong>${esc(g.grind)}</strong> — avg ${g.avgTime.toFixed(1)}s${g.avgRating ? ' · ' + g.avgRating.toFixed(1) + '★' : ''} <span class="gr-alt-note">${g.avgTime < ESPRESSO_MIN ? 'too fast (coarser?)' : g.avgTime > ESPRESSO_MAX ? 'too slow (finer?)' : 'in range'}</span></div>`;
    }).join('');

    const bestIcon = best.inRange ? '🟢' : (best.avgTime < ESPRESSO_MIN ? '🔵' : '🔴');
    const bestHint = best.inRange
      ? 'On target — extraction in 25–35s'
      : best.avgTime < ESPRESSO_MIN
        ? `Avg ${best.avgTime.toFixed(1)}s — slightly fast, try going finer`
        : `Avg ${best.avgTime.toFixed(1)}s — slightly slow, try going coarser`;

    html += `<div class="gr-card">
      <div class="gr-card-head">
        <span class="gr-card-title">⚙️ ${esc(grinder)}</span>
        <span class="gr-badge">${grinds.length} setting${grinds.length > 1 ? 's' : ''} logged</span>
      </div>
      <div class="gr-best">
        <div class="gr-best-label">Best Grind Setting</div>
        <div class="gr-best-value">${bestIcon} <strong>${esc(best.grind)}</strong></div>
        <div class="gr-best-hint">${bestHint}${best.avgRating ? ' · ' + best.avgRating.toFixed(1) + '★ avg rating' : ''} · ${best.sessions} shot${best.sessions > 1 ? 's' : ''}</div>
      </div>
      ${extraHtml ? `<div class="gr-alts"><div class="gr-alts-label">Other settings tried</div>${extraHtml}</div>` : ''}
    </div>`;
  });

  el.innerHTML = html || '<p style="font-size:.78rem;color:var(--light);font-style:italic">No espresso sessions with timed extractions found.</p>';
}

function renderMoneySaved() {
  const { rows, totalHomeCost, totalCafeCost, totalSaved } = calcMoneySaved();

  $('ins-saved-total').textContent = (totalSaved >= 0 ? '₹' : '-₹') + Math.abs(totalSaved).toLocaleString('en-IN', { maximumFractionDigits: 0 });
  $('ins-home-cost').textContent = fmt(totalHomeCost);
  $('ins-cafe-cost').textContent = fmt(totalCafeCost);

  // ── Break-even panel ─────────────────────────────────
  renderBreakEven(totalSaved, rows);

  // Sort rows: tracked sessions first (date desc), then untracked bags
  const sorted = [...rows].sort((a, b) => {
    if (a.tracked !== b.tracked) return a.tracked ? -1 : 1;
    return new Date(b.date) - new Date(a.date);
  });

  if (!sorted.length) {
    $('ins-savings-breakdown').innerHTML = '<p style="font-size:.78rem;color:var(--light);font-style:italic">Log some beans or brew sessions to see savings.</p>';
    $('ins-savings-assumptions').textContent = '';
    return;
  }

  // Group tracked vs untracked
  const tracked = sorted.filter(r => r.tracked);
  const untracked = sorted.filter(r => !r.tracked);
  let html = '';

  if (tracked.length) {
    html += `<div style="font-size:.67rem;color:var(--mid);letter-spacing:.12em;text-transform:uppercase;font-weight:500;margin:.3rem 0 .5rem">📓 Tracked brew sessions (${tracked.length})</div>`;
    tracked.forEach(r => {
      const savedColor = r.saved >= 0 ? 'var(--green)' : '#c0392b';
      html += `<div class="savings-row">
            <div class="savings-row-label">
              ${esc(r.label)}
              <small>${fmtDate(r.date)} · ${r.note}</small>
            </div>
            <div style="text-align:right;flex-shrink:0">
              <div style="font-family:'Playfair Display',serif;font-size:.88rem;color:${savedColor}">${r.saved >= 0 ? '+₹' : '-₹'}${Math.abs(r.saved).toFixed(0)}</div>
              <div style="font-size:.63rem;color:var(--light)">saved</div>
            </div>
          </div>`;
    });
  }

  if (untracked.length) {
    html += `<div style="font-size:.67rem;color:var(--mid);letter-spacing:.12em;text-transform:uppercase;font-weight:500;margin:.8rem 0 .5rem">🫙 Untracked bags — estimated (${untracked.length})</div>`;
    untracked.forEach(r => {
      const savedColor = r.saved >= 0 ? 'var(--green)' : '#c0392b';
      html += `<div class="savings-row">
            <div class="savings-row-label">
              ${esc(r.label)}
              <small>${fmtDate(r.date)} · ${r.note}</small>
            </div>
            <div style="text-align:right;flex-shrink:0">
              <div style="font-family:'Playfair Display',serif;font-size:.88rem;color:${savedColor}">${r.saved >= 0 ? '+₹' : '-₹'}${Math.abs(r.saved).toFixed(0)}</div>
              <div style="font-size:.63rem;color:var(--light)">saved</div>
            </div>
          </div>`;
    });
  }

  $('ins-savings-breakdown').innerHTML = html;
  $('ins-savings-assumptions').innerHTML =
    `<span style="font-weight:600;color:var(--caramel)">☕ Café price assumptions:</span> ` +
    `V60 / Pour Over = ₹${MARKET_V60} · Espresso Machine = ₹${MARKET_ESPRESSO} · Americano = ₹${MARKET_AMERICANO} · Kompresso = ₹${MARKET_KOMPRESSO} · French Press / KaldiPress / AeroPress = ₹${MARKET_FRENCH_KALDI} · Cold Brew = ₹${MARKET_COLD_BREW} · Moka Pot = ₹${MARKET_MOKA_POT} · 🥛 Milk-based = ₹${MARKET_MILK_BASED} · Base/Other = ₹${MARKET_STANDARD} per cup. ` +
    `Standard dose ${STD_DOSE_G}g/cup for untracked bags. Home cost = bag price ÷ bag size. Negative savings = brew cost more than café.`;
}

function renderBreakEven(totalSaved, rows) {
  // Button toggle state — default: exclude untracked (tracked sessions only)
  const btn = $('be-include-untracked');
  if (btn && !btn.dataset.initialized) {
    btn.dataset.initialized = 'true';
    const stored = localStorage.getItem('be_include_untracked');
    const active = stored === 'true'; // default false if nothing stored
    btn.dataset.active = active ? 'true' : 'false';
    _applyBeToggleStyle(btn, active);
  }

  const includeUntracked = btn ? btn.dataset.active === 'true' : false;

  // Gear investment = gears + accessories spend
  const gearLogs = logs.filter(l => l.category === 'gears' || l.category === 'accessories');
  const gearInvestment = gearLogs.reduce((s, l) => s + Number(l.price || 0), 0);

  // Active savings rows based on untracked toggle
  const activeRows = rows ? rows.filter(r => includeUntracked || r.tracked) : [];

  /* =================================================================
   * CHRONOLOGICAL SIMULATION
   * Walk through all dates in order, applying gear purchases then
   * savings accrual. This correctly handles:
   *   - A new gear purchase resetting break-even state
   *   - Multiple break-even crossings over time
   *   - Accurate "achieved" that isn't inflated by estimated future savings
   * ================================================================= */
  const allDateSet = new Set([
    ...gearLogs.map(g => g.date),
    ...activeRows.map(r => r.date),
  ].filter(Boolean));
  const allDates = [...allDateSet].sort();

  let runningInvestment = 0;
  let runningSavings = 0;
  let breakEvenDate = null;  // date of most recent positive crossing
  let lastGearDate = null;   // date of most recent gear purchase

  for (const d of allDates) {
    // Process gear purchases FIRST on this day
    const dayInv = gearLogs
      .filter(g => g.date === d)
      .reduce((s, g) => s + Number(g.price || 0), 0);

    if (dayInv > 0) {
      lastGearDate = d;
      runningInvestment += dayInv;
      // New purchase may push net negative — clear the break-even milestone
      if (runningSavings < runningInvestment) {
        breakEvenDate = null;
      }
    }

    // Then add savings for this day
    const daySav = activeRows
      .filter(r => r.date === d)
      .reduce((s, r) => s + Number(r.saved || 0), 0);
    runningSavings += daySav;

    // Check if we crossed into positive territory today
    if (runningInvestment > 0 && runningSavings >= runningInvestment && !breakEvenDate) {
      breakEvenDate = d;
    }
  }

  // Final state from the simulation (not inflated by estimated future savings)
  const finalNet = runningSavings - runningInvestment;
  const achieved = gearInvestment > 0 && finalNet >= 0;
  const netPosition = finalNet;
  const pct = gearInvestment > 0
    ? Math.min(100, (runningSavings / runningInvestment) * 100)
    : (runningSavings > 0 ? 100 : 0);
  const remaining = achieved ? 0 : runningInvestment - runningSavings;

  // ── ETA text ────────────────────────────────────────────────────────
  let etaHtml = '';
  if (!achieved && gearInvestment > 0) {
    // Rate calculation: use savings since the most recent gear purchase
    // (the most relevant window — tells us how fast we're clearing the latest debt)
    const rateAnchorDate = lastGearDate || (allDates.length > 0 ? allDates[0] : null);

    if (rateAnchorDate) {
      const today = new Date();
      const anchorMs = new Date(rateAnchorDate + 'T00:00:00').getTime();
      const spanDays = Math.max(1, Math.floor((today - anchorMs) / 86400000));
      const savingsSinceAnchor = lastGearDate
        ? activeRows.filter(r => r.date >= lastGearDate).reduce((s, r) => s + Number(r.saved || 0), 0)
        : runningSavings;
      const dailySavings = savingsSinceAnchor / spanDays;

      if (dailySavings > 0) {
        const daysLeft = Math.ceil(remaining / dailySavings);
        const beDate = new Date();
        beDate.setDate(beDate.getDate() + daysLeft);
        const beDateStr = beDate.toLocaleDateString('en-IN', { day: '2-digit', month: 'short', year: 'numeric' });
        const months = Math.round(daysLeft / 30);
        etaHtml = `<span style="font-size:1.1rem">📅</span>
              <span>At your current pace of <strong style="color:var(--caramel)">&#8377;${dailySavings.toFixed(0)}/day</strong> in savings,
              you'll break even in <strong style="color:var(--roast)">${months > 1 ? months + ' months' : daysLeft + ' days'}</strong>
              — around <strong>${beDateStr}</strong>.</span>`;
      } else {
        etaHtml = `<span>No savings recorded since the last gear purchase. Log brew sessions to track progress.</span>`;
      }
    } else {
      etaHtml = `<span>Log brew sessions to start calculating your savings rate.</span>`;
    }
  } else if (achieved) {
    const dateStr = breakEvenDate ? fmtDate(breakEvenDate) : '';
    etaHtml = `<span style="font-size:1.1rem">🥳</span>
          <span>You've <strong style="color:var(--green)">broken even!</strong> Every cup you brew now is pure profit over café prices.
          You're <strong style="color:var(--green)">&#8377;${netPosition.toLocaleString('en-IN', { maximumFractionDigits: 0 })}</strong> ahead.
          ${dateStr ? `<br><small style="color:var(--light);font-size:0.75rem;margin-top:0.25rem;display:inline-block">🔓 Milestone reached on: <strong>${dateStr}</strong></small>` : ''}</span>`;
  } else if (gearInvestment === 0) {
    etaHtml = `<span>No gear or accessories logged yet. Add them to the log to track your investment.</span>`;
  } else {
    etaHtml = `<span>Log brew sessions to start calculating your savings rate.</span>`;
  }

  // Update DOM
  $('be-card').classList.toggle('achieved', achieved);
  $('be-badge').className = 'be-badge ' + (achieved ? 'achieved' : 'in-progress');
  $('be-badge').textContent = achieved ? '✓ Broken Even!' : 'In Progress';
  $('be-fill').style.width = pct.toFixed(1) + '%';
  $('be-fill').className = 'be-fill' + (achieved ? ' done' : '');
  $('be-progress-pct').textContent = pct.toFixed(0) + '% recovered';
  $('be-progress-target').textContent = fmt(gearInvestment) + ' invested';
  $('be-gear-cost').textContent = fmt(gearInvestment);
  $('be-saved').textContent = fmt(runningSavings);
  $('be-remaining').textContent = achieved
    ? '+' + fmt(netPosition)
    : fmt(remaining);
  $('be-remaining').style.color = achieved ? 'var(--green)' : 'var(--caramel)';
  $('be-remaining-label').textContent = achieved ? 'In profit' : 'Still to recover';
  $('be-eta').innerHTML = etaHtml;
}


/* ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   BOOTSTRAP
──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────── */
/* ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   THEME SYSTEM
──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────── */
function renderAll() { renderStats(); renderEntries(); }


/* ═══════════════════════════════════════════════════════════
   JOURNAL PAGINATION
═══════════════════════════════════════════════════════════ */
let jPage = 0;
const J_PER_PAGE = 10;

// Wrap existing renderJournal to add pagination
const _renderJournalOrig = renderJournal;
function renderJournal() {
  // Stats
  const total = journal.length;
  const rated = journal.filter(j => j.rating > 0);
  const avgR = rated.length ? (rated.reduce((s, j) => s + j.rating, 0) / rated.length).toFixed(1) : '—';
  const brCount = {};
  journal.forEach(j => { if (j.brewer) brCount[j.brewer] = (brCount[j.brewer] || 0) + 1; });
  const favBr = Object.entries(brCount).sort((a, b) => b[1] - a[1])[0]?.[0] || '—';
  if ($('jsTotal')) $('jsTotal').textContent = total;
  if ($('jsAvgRating')) $('jsAvgRating').textContent = avgR;
  if ($('jsFavMethod')) $('jsFavMethod').textContent = favBr.split(' ')[0];
  // Savings
  const { totalSaved } = calcMoneySaved();
  if ($('jsSaved')) $('jsSaved').textContent = (totalSaved >= 0 ? '₹' : '-₹') +
    Math.abs(totalSaved).toLocaleString('en-IN', { maximumFractionDigits: 0 });

  const sorted = [...journal].sort((a, b) => new Date(b.date) - new Date(a.date));
  const totalPages = Math.max(1, Math.ceil(sorted.length / J_PER_PAGE));
  jPage = Math.min(jPage, totalPages - 1);
  const slice = sorted.slice(jPage * J_PER_PAGE, (jPage + 1) * J_PER_PAGE);

  if (!slice.length) {
    if ($('journalList')) $('journalList').innerHTML = '<div class="empty-state"><div class="icon">📓</div><p>No brew sessions yet. Log your first cup!</p></div>';
    if ($('journalPagination')) $('journalPagination').innerHTML = '';
    return;
  }

  if ($('journalList')) {
    $('journalList').innerHTML = slice.map(j => {
      const stars = '★'.repeat(j.rating || 0) + '☆'.repeat(5 - (j.rating || 0));
      const tastes = (j.tastes || []).map(t => `<span class="taste-chip">${esc(t)}</span>`).join('');
      const metaBrewer = [];
      if (j.brewer) metaBrewer.push(`<span class="epill brew">☕ ${esc(j.brewer)}</span>`);
      if (j.is_milk_based) metaBrewer.push(`<span class="epill" style="color:var(--caramel);font-weight:600">🥛 Milk-based</span>`);

      const metaExtract = [];
      if (j.dose) metaExtract.push(`<span class="epill">${j.dose}g dose</span>`);
      if (j.yield) metaExtract.push(`<span class="epill">${j.yield}g yield</span>`);
      if (j.time) metaExtract.push(`<span class="epill">${j.time}s</span>`);
      if (j.temp) metaExtract.push(`<span class="epill">${j.temp}°C</span>`);

      const metaGrinder = [];
      if (j.grinder) metaGrinder.push(`<span class="epill">⚙️ ${esc(j.grinder)}</span>`);
      if (j.grind) metaGrinder.push(`<span class="epill">📏 ${esc(j.grind)}</span>`);

      const beanName = j.beanId ? logs.find(l => l.id === parseInt(j.beanId))?.name || j.beanLabel || '' : j.beanLabel || '';
      return `<div class="journal-entry">
    <div class="je-header">
      <div>
        <div class="je-title">${beanName ? esc(beanName) : 'Brew Session'}</div>
        <div style="font-size:.72rem;color:var(--caramel);margin-top:.1rem">${stars}</div>
      </div>
      <div class="je-date">${fmtDate(j.date)}</div>
    </div>
    ${metaBrewer.length ? `<div class="je-meta" style="margin-top:.4rem">${metaBrewer.join('')}</div>` : ''}
    ${metaExtract.length ? `<div class="je-meta" style="margin-top:.2rem">${metaExtract.join('')}</div>` : ''}
    ${metaGrinder.length ? `<div class="je-meta" style="margin-top:.2rem">${metaGrinder.join('')}</div>` : ''}
    ${tastes ? `<div class="tasting-chips" style="margin-top:.4rem">${tastes}</div>` : ''}
    ${j.notes ? `<div class="je-notes">${esc(j.notes)}</div>` : ''}
    <div class="je-actions">
      <button class="action-btn share-btn" onclick="shareJournalEntry(${j.id})">↗ Share</button>
      <button class="action-btn" onclick="openJournalModal(${j.id})">Edit</button>
      <button class="action-btn del" onclick="deleteJournalEntry(${j.id})">✕</button>
    </div>
  </div>`;
    }).join('');
  }

  // Pagination controls
  const pagEl = $('journalPagination');
  if (pagEl) {
    if (totalPages > 1) {
      pagEl.innerHTML = `<div class="pag-bar">
        <button class="pag-btn" onclick="jPage=Math.max(0,jPage-1);renderJournal()" ${jPage === 0 ? 'disabled' : ''}>← Prev</button>
        <span class="pag-info">Page ${jPage + 1} of ${totalPages} · ${total} sessions</span>
        <button class="pag-btn" onclick="jPage=Math.min(${totalPages - 1},jPage+1);renderJournal()" ${jPage === totalPages - 1 ? 'disabled' : ''}>Next →</button>
      </div>`;
    } else {
      pagEl.innerHTML = `<div class="pag-info" style="text-align:center;padding:.5rem 0;font-size:.75rem;color:var(--mid)">${total} session${total !== 1 ? 's' : ''}</div>`;
    }
  }
}

/* ═══════════════════════════════════════════════════════════
   OWNER ACCESS & AUTH
═══════════════════════════════════════════════════════════ */
const CORRECT_PIN_HASH = 'f652f5c87c84f33899f9be3b2f62607ce5b61c68cac1f70bb4cdbb40d79b6904'; // PIN: 2811
const SESSION_KEY = 'brewlog_unlocked';
let isOwner = false;
let pinInput = '';

function applyAccessLevel() {
  if (isOwner) {
    document.body.classList.remove('visitor-mode');
  } else {
    document.body.classList.add('visitor-mode');
  }
  document.querySelectorAll('.owner-btn').forEach(btn => {
    btn.textContent = isOwner ? '🔓' : '🔐';
    btn.title = isOwner ? 'Owner mode – click to exit' : 'Owner Login';
  });
}

function toggleOwnerAccess() {
  if (isOwner) {
    isOwner = false;
    sessionStorage.removeItem(SESSION_KEY);
    applyAccessLevel();
  } else {
    showPinModal();
  }
}

function showPinModal() {
  pinInput = '';
  updateDots();
  document.getElementById('pin-overlay').classList.add('visible');
}
function hidePinModal() {
  document.getElementById('pin-overlay').classList.remove('visible');
}
function maybeHidePinModal(e) {
  if (e.target === document.getElementById('pin-overlay')) hidePinModal();
}

function updateDots(state) {
  for (let i = 0; i < 4; i++) {
    const d = document.getElementById('d' + i);
    if (!d) continue;
    d.className = 'pin-dot';
    if (state === 'ok') d.className = 'pin-dot filled ok';
    else if (state === 'error') d.className = 'pin-dot filled error';
    else if (i < pinInput.length) d.className = 'pin-dot filled';
  }
}

async function pinPress(digit) {
  if (pinInput.length >= 4) return;
  pinInput += digit;
  document.getElementById('pinErr').textContent = '';
  updateDots();
  if (pinInput.length === 4) await checkPin();
}
function pinDel() {
  pinInput = pinInput.slice(0, -1);
  document.getElementById('pinErr').textContent = '';
  updateDots();
}

async function sha256(msg) {
  const buf = await crypto.subtle.digest('SHA-256', new TextEncoder().encode(msg));
  return Array.from(new Uint8Array(buf)).map(b => b.toString(16).padStart(2, '0')).join('');
}

async function checkPin() {
  const hash = await sha256(pinInput);
  if (hash === CORRECT_PIN_HASH) {
    updateDots('ok');
    isOwner = true;
    sessionStorage.setItem(SESSION_KEY, 'owner');
    setTimeout(() => {
      const overlay = document.getElementById('pin-overlay');
      overlay.classList.add('unlocking');
      setTimeout(() => overlay.classList.remove('visible', 'unlocking'), 400);
      applyAccessLevel();
      // If on add page, reload to show the form
      if (document.body.dataset.page === 'add') window.location.reload();
    }, 350);
  } else {
    updateDots('error');
    document.getElementById('pinErr').textContent = 'Incorrect PIN. Try again.';
    setTimeout(() => { pinInput = ''; updateDots(); }, 800);
  }
}

document.addEventListener('keydown', e => {
  if (!document.getElementById('pin-overlay').classList.contains('visible')) return;
  if (e.key >= '0' && e.key <= '9') pinPress(e.key);
  else if (e.key === 'Backspace') pinDel();
  else if (e.key === 'Escape') hidePinModal();
});

/* ═══════════════════════════════════════════════════════════
   THEME
═══════════════════════════════════════════════════════════ */
function toggleTheme() {
  const isDark = document.body.classList.toggle('dark-mode');
  localStorage.setItem('theme', isDark ? 'dark' : 'light');
  updateThemeUI();
  // Re-render insights charts on theme change (only if we're on the insights page)
  if (document.body.dataset.page === 'insights') renderInsights();
}
function applyTheme() {
  const saved = localStorage.getItem('theme');
  const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
  if (saved === 'dark' || (!saved && prefersDark)) document.body.classList.add('dark-mode');
  updateThemeUI();
}
function updateThemeUI() {
  const isDark = document.body.classList.contains('dark-mode');
  document.querySelectorAll('.theme-toggle').forEach(btn => {
    btn.innerHTML = isDark ? '☀️' : '🌙';
  });
}

/* ═══════════════════════════════════════════════════════════
   PAGE-AWARE BOOT
═══════════════════════════════════════════════════════════ */
document.addEventListener('DOMContentLoaded', async () => {
  // Restore session
  if (sessionStorage.getItem(SESSION_KEY) === 'owner') isOwner = true;

  applyTheme();
  applyAccessLevel();

  const page = document.body.dataset.page || 'insights';

  // Add-page: guard non-owners before even loading data
  if (page === 'add' && !isOwner) {
    const main = document.getElementById('add-main');
    if (main) main.innerHTML = `<div style="display:flex;flex-direction:column;align-items:center;justify-content:center;min-height:55vh;text-align:center;padding:2rem">
      <div style="font-size:3rem;margin-bottom:1rem">🔐</div>
      <div style="font-family:'Playfair Display',serif;font-size:1.4rem;color:var(--roast);margin-bottom:.5rem">Owner access required</div>
      <div style="font-size:.8rem;color:var(--mid);margin-bottom:1.5rem">Log in to add or edit orders.</div>
      <button class="btn-primary" onclick="showPinModal()">🔐 Log In</button>
    </div>`;
    return; // skip data load
  }

  // Add-page pre-setup
  if (page === 'add') {
    if ($('inp-date')) $('inp-date').valueAsDate = new Date();
    if ($('itemsList')) addItemRow();
  }

  await loadData();

  switch (page) {
    case 'insights': renderInsights(); break;
    case 'log': renderAll(); break;
    case 'shelf': renderShelf(); break;
    case 'process': renderProcess(); break;
    case 'journal': renderJournal(); break;
    case 'add': {
      const editId = new URLSearchParams(window.location.search).get('edit');
      if (editId) {
        // pre-fill form for editing
        const entry = logs.find(l => l.id === parseInt(editId));
        if (entry) {
          editingId = parseInt(editId);
          if ($('addPageTitle')) $('addPageTitle').innerHTML = 'Edit <em>Entry</em>';
          if ($('editBanner')) { $('editBanner').textContent = `Editing "${entry.name}". Update and save.`; $('editBanner').classList.add('active'); }
          if ($('inp-date')) $('inp-date').value = entry.date;
          if ($('inp-order')) $('inp-order').value = entry.order_id || '';
          if ($('inp-vendor')) $('inp-vendor').value = entry.vendor;
          const isCombo = entry.is_combo === 1;
          if ($('inp-combo')) { $('inp-combo').checked = isCombo; }
          if ($('inp-combo-price')) $('inp-combo-price').value = entry.combo_price || '';
          if ($('comboPriceWrap')) $('comboPriceWrap').classList.toggle('active', isCombo);
          if ($('itemsList')) { $('itemsList').innerHTML = ''; itemCount = 0; addItemRow(entry); }
        }
      }
      break;
    }
  }
});
